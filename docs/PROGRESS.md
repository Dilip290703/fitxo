# Fitzo — Build Progress

**This is the living source of truth for what's done.** Claude Code reads this before
starting work and updates it when finishing a task (via `/finish-task`).

Status legend: `[ ]` not started · `[~]` in progress / partial · `[x]` built & merged · `[T]` tested
Owner: put initials (e.g. `J` Jay / `A` Amit) next to in-progress items.

Last updated: 2026-07-10 (repo audit + customer UI refresh round 2 — see Decisions log)

> **Status (2026-06-04):** Everything below is merged to `main` (no open PRs).
> **Customer panel: standalone screens complete** — Dilip wrapped his customer work
> (Order History, Notifications, Search, Brand pages, How It Works, Size Guide, 404,
> plus the login + navy-button fixes). The core **order → try (rider waits) → keep/return** loop
> is wired (checkout creates a real order + try_session; confirmation + tracking +
> keep/return work). Remaining customer items are flow/Razorpay: **Payment (#13)**,
> Return Pickup (#14), Time Slot (#8), AI Skin Setup (#18) — owned by partner.
> **Store panel (P3) COMPLETE (2026-06-10) — owned by D.** All 14 screens merged +
> browser-verified, with 5 store migrations (004 manager-read, 005 RLS security fix,
> 006 product-write, 007 mark-ready, 008 profile/staff) and seeds under
> `apps/store/seed/`. Store-side writes go through guarded SECURITY DEFINER RPCs.
> Agent panel + Razorpay — partner.
> No automated tests yet. Stack is Supabase (no Firebase).

---

## Foundation
- [x] Monorepo restructure — 4 app folders + shared packages, pnpm workspaces (see Decisions log)
- [ ] 4 hosting projects + subdomains wired (host TBD)
- [x] Supabase schema — full loop now present incl. `try_sessions`, `returns`, `payments`, `payouts` (migration 002) + `order_items` update policy (003).
- [~] RLS policies — enabled across tables; ⚠️ **store-manager visibility of orders containing their products may need a policy** (order_items has no store_id; store sees orders via products.store_id join). Verify/add during Store panel build.
- [~] Auth — customer email+password & phone OTP work; admin email+password works (no 2FA); **store/agent auth not built yet** (store login is the first Store-panel task).
- [~] Razorpay integration (payments) — customer **per-item Keep payment** built & browser-verified in test mode (J, 2026-06-19): `razorpay` SDK + `apps/customer/lib/razorpay.ts` + create/confirm server actions + Razorpay Checkout modal. Settlement goes through a SECURITY DEFINER RPC `confirm_keep_payment` (migration **009**) that re-verifies the HMAC **in-DB** (pgcrypto); secret stored in **Supabase Vault** (`razorpay_key_secret`). `payment.captured` **webhook built 2026-07-10** (migration 039, pending apply — see Known issues); payouts still TODO.
- [ ] Razorpay Payouts integration (store + agent) — **investigated 2026-07-10: blocked on business steps, not code** (RazorpayX needs a registered entity + own account/KYC; Route is RBI-gated behind ₹40L turnover). Full verdict + go-live checklist + integration sketch in **`docs/PAYOUTS-GOING-LIVE.md`**; manual ledger hardened meanwhile (migration 042).
- [ ] AI skin-tone endpoint — only DB columns + hardcoded display; no endpoint
- [~] Shared UI kit — `packages/ui` exists but thin; admin has its own `components/admin/*`

## P1 — Customer Panel (25)
- [x] 1. Homepage / Landing
- [x] 2. Product Listing Page
- [x] 3. Product Detail Page  *(Supabase-wired; AI skin-tone badge still placeholder)*
- [x] 4. Login / Signup Page  *(LoginPanel, Supabase auth)*
- [~] 5. OTP Verification  *(handled inline in LoginPanel; no standalone screen)*
- [x] 6. Cart / Bag Page  *(CartProvider; localStorage)*
- [x] 7. Checkout — Address Page  *(creates a real order + try_session in Supabase — partner)*
- [ ] 8. Checkout — Delivery-Slot Booking  *(now core to the model — customer picks an available slot; partner)*
- [x] 9. Order Confirmation Page  *(partner)*
- [x] 10. Order Tracking Page  *(timeline + try-window countdown — partner)*
- [~] 11. Try Timer  *(the try-window countdown is built into Order Tracking; no standalone screen)*
- [x] 12. Keep or Return  *(keep/return actions live in Order Tracking — partner)*
- [T] 13. Payment — per-item Razorpay Keep payment — J  *(tap **Keep** on the order-tracking page → Razorpay Checkout → in-DB-verified settle → item flips to "Keeping" + order `payment_status='paid'`. Browser-verified in test mode via UPI `success@razorpay`. Needs migrations 009+010 + the Vault secret. See `docs/HANDOFF-payment.md`.)*
- [~] 14. Return Pickup Scheduling  *(largely obsolete under the new model — returns are handed back to the waiting rider on the spot, no scheduling. Keep only for post-purchase refund returns; partner)*
- [x] 15. Profile Page
- [x] 16. Wishlist Page
- [x] 17. Order History Page — D
- [ ] 18. AI Skin Tone Setup Page  *(placeholder — blocked on AI endpoint; **removed from the Profile page 2026-07-10 per owner** — no longer linked from account UI, route still exists standalone)*
- [x] 19. Notifications Page — D
- [x] 20. Brand / Store Page — D
- [x] 21. Search Results Page — D
- [x] 22. How It Works Page — D
- [x] 23. Contact / Support Page
- [x] 24. Size Guide — D
- [x] 25. 404 Error Page — D

## P2 — Delivery Agent Panel (12) — CORE LOOP BUILT & MERGED TO MAIN (J, 2026-06-21)
> Agent app runs on **:3002** (`pnpm dev:agent`). Rider = role `rider` + verified `riders` row
> (mirrors store-manager gate via `lib/agent-auth.ts`). Rider writes go through guarded
> SECURITY DEFINER RPCs (migration **014**). Live coordination via **Supabase Realtime** on
> `orders`/`try_sessions`/`deliveries`. Needs migration 014 + a verified test rider (see Known issues).
> SSR localStorage shim (`instrumentation.ts`) added to both `apps/agent` and `apps/store`
> (Node 22+ ships a broken `globalThis.localStorage` that crashes @supabase/ssr during render).
> Merged straight to `main` 2026-06-21 (owner-authorized; logic reviewed, browser test still recommended).
> **Testing needs ZERO Supabase SQL now** after a one-time setup — see `docs/HANDOFF-agent-testing.md`.
> Migration **015** auto-provisions `public.users` + a `riders` row on signup (a new rider used to
> need a manual INSERT to be visible in Admin → that was the "rider can't log in" SQL). Verify +
> assign already live in Admin (Riders → Verify Rider; Deliveries → Assign).
>
> **Full panel rebuild (J, 2026-06-26):** the agent app is no longer a 2-screen shell.
> A responsive `AgentShell` (desktop sidebar + mobile bottom-nav + slide-over "More")
> now wraps **9 navigable screens** matching the store/admin quality bar, all wired to
> Supabase via `lib/useAgentGuard.ts` (shared gate hook) + `lib/agent-data.ts` (earnings,
> history, notifications). The global online/offline toggle lives in the shell. Earnings =
> the order's `delivery_fee` per completed delivery (no rider-commission config exists yet).
> New migration **017** adds a guarded `rider_update_profile` RPC for vehicle edits and
> drops the over-broad `riders_update_own` policy (it allowed a client to self-set
> `is_verified`). `pnpm --filter @fitzo/agent build` passes (14 routes). Browser test still recommended.
>
> **REWORK — real-world rider app (D, 2026-07-06..07, branch `feature/agent-panel-rework`):**
> full audit (`docs/AGENT_PANEL_AUDIT.md`) then 4 phases — paper/ink/gold design system at
> rider scale + mobile-first shell + SVG icons + PWA install (Phase 1); the delivery flow made
> real (Phase 2, migration **033**: pickup store on offers/detail, 1-job cap, PII-redacted offer
> feed, arrival step + doorstep OTP, collect-returns checklist, wake-lock, rider fail/report
> paths → Admin > Complaints, status-guarded completion); money (Phase 3, migration **034**:
> rider bank/UPI+PAN capture, Earnings wired to `agent_payouts` with Paid/Due chips + payout
> history, admin payout destination, COD hidden at checkout); polish (Phase 4: live support
> tickets on `complaints`, honest error+retry states on every list, one unified mute, guide/FAQ
> rewritten for the new flow). **Migrations 033–034 pending apply; full live-fire is Dilip's step.**
- [x] 1. Agent Login — J  *(email+pw sign-in + sign-up + forgot/reset password; verified-rider gate with pending/not-rider screens.)*
- [x] 2. Agent Dashboard / Home — J  *(rebuilt: greeting, online toggle, today's stats (active/done/earned/rating), new-jobs-to-accept + active deliveries + this-week earnings teaser.)*
- [ ] 3. Order Detail (Pickup)  *(folded into the single Delivery Detail status machine)*
- [x] 4. Delivery Detail + status machine — J  *(accept → picked_up (order out_for_delivery) → **mark delivered** (order delivered) → customer accepts → live 7-min timer + live keep/return decisions → collect returns & complete. This is the loop. Customer side: realtime "order arrived → start 7-min window" popup + `start_try_window` RPC.)*
- [~] 5. Return Collection — J  *(returns are surfaced + counted inside Delivery Detail's try-window panel; no separate screen)*
- [ ] 6. Navigation / Map Screen  *(currently an "Open in Maps" link on Delivery Detail — no Maps key yet)*
- [x] 7. Agent Earnings — J  *(/earnings: this-week hero + 7-day CSS bar chart, today/month/all-time/avg rollups, recent-payouts ledger; pay = order delivery_fee per completed job.)*
- [x] 8. Agent Profile — J  *(/profile: avatar, verified badge, rating/deliveries/status stats, vehicle card, account rows.)*
- [x] 9. Agent Notifications — J  *(/notifications: reads `notifications` table, unread count, tap-to-read + mark-all-read.)*
- [x] 10. Order History — J  *(/history: completed/failed deliveries grouped by day with per-job fee + all/completed/failed filter.)*
- [x] 11. Support — J  *(/support: helpline + email cards + rider FAQ accordion.)*
- [x] 12. Onboarding / Guide — J  *(/guide: 6-step try-at-home walkthrough; + /settings for availability, vehicle edit via rider_update_profile RPC, and password change.)*

## P3 — Store Panel (14) — ✅ COMPLETE, owned by D (built week of 2026-06-04..10; all 14 merged + browser-verified)
> Build order: Login → Dashboard → Catalog → Add/Edit Product → Order Management → Order Detail → rest.
> Data model: store user = role `store_manager` + `store_managers` row (user↔store); products via `products.store_id`; a store's orders via `order_items → products WHERE store_id = mine` (no store_id on order_items). Store dev server runs on **:3003** (`pnpm dev:store`).
>
> **REWORK — real-world ready (D, week of 2026-07-02):** the panel stopped being demo-only.
> **Self-serve store onboarding** (browser-verified): public seller signup on the store login
> (`role=store_manager` + `store_name` metadata → migration **029**'s `handle_new_user` auto-provisions
> a DRAFT store + `store_managers` link) → 5-step wizard (store details → pickup address → business/tax →
> payout → review; client + in-RPC validation) → submit → **admin approves/rejects (+reason, resubmittable)**
> on Admin > Stores (queue badge + KYC/bank panel, audit-logged). `useStoreGuard` now requires an APPROVED
> store for every live screen (non-approved → `/onboarding`); the old static seller guide moved to `/guide`.
> Sensitive KYC/bank data lives in the **private `store_business_details` table** (RLS: own manager or
> admin only — never on world-readable `stores`); store writes go through guarded RPCs
> `save_store_onboarding` / `submit_store_onboarding` (can't self-approve). Forgot/reset-password added.
- [T] 1. Store Login — D  *(email+pw + store_manager gate, reusable `getStoreContext`, auth-gated root stub; login verified across all conditions in a real browser; PR #11 open)*
- [T] 2. Store Dashboard — D  *(sidebar shell + KPI cards + low-stock & recent-orders panels; migrations 004 & 005 applied; verified in a real browser with seed data — store-scoped RLS confirmed; PR #12 open)*
- [~] 3. Product Catalog Page — D  *(store's own products: table + search/status filter + activate/deactivate + soft-delete; Add/Edit entry points stubbed "Soon" pending #4/#5; layout verified via preview with seed data, write actions pending real-browser test)*
- [T] 4. Add Product Page — D  *(combined with #5 — shared ProductForm at /catalogue/new; migration 006 applied; verified in browser)*
- [T] 5. Edit Product Page — D  *(combined with #4 — shared ProductForm at /catalogue/[id]/edit; RESTRICT-safe variant edits; verified in browser. Images deferred.)*
- [T] 6. Order Management Page — D  *(combined with #7 — /orders list, RLS-scoped, status-bucket filter, per-store subtotal + ready/kept/returned counts; migration 007 applied; verified in browser. Rows are now fully clickable with a › affordance.)*
- [T] 7. Order Detail (Store) — D  *(combined with #6 — /orders/[id]; store's line items + SKU + keep/return outcome; per-item "Mark ready" + "Mark all ready" via guarded set_order_item_prepared RPC; verified in browser. No customer PII (RLS).)*
- [T] 8. Returns Management — D  *(combined with #9/#10 — /returns: read-only tracking (lifecycle owned by agent/admin flow), status filter, condition badges; multi-store leak closed client-side; verified in browser with seed)*
- [T] 9. Earnings Page (Store) — D  *(/earnings: NET-of-commission money summary — reads `system_settings.commission_rate` since 2026-07-02 and mirrors Admin > Store Payouts math — + gross−commission breakdown, payouts ledger, recent kept items; verified in browser)*
- [T] 10. Analytics Page (Store) — D  *(/analytics: 30-day CSS bar charts, keep-vs-return rate, top products; verified in browser with seed)*
- [T] 11. Store Profile Settings — D  *(/settings: contact/description/address editable via guarded update_store_profile RPC (migration 008); name/slug/verified stay admin-owned; verified in browser)*
- [T] 12. Staff Management — D  *(/staff: read-only roster via get_store_staff RPC (migration 008 — co-managers' names/emails without widening users RLS); add/remove admin-provisioned; verified in browser)*
- [T] 13. Support Page (Store) — D  *(/support: partner contact + FAQ accordion; static — no complaints table exists yet; verified in browser)*
- [T] 14. Store Onboarding — D  *(/onboarding "Guide": 6-step seller walkthrough; sidebar fully live, no "Soon" chips left; verified in browser)*

## P4 — Admin Panel (21)
> Admin is the most-built panel: 20 of 21 page components are Supabase-wired. Verified by
> static inspection (renders + queries), **not** click-tested. Bonus routes beyond the spec
> list also exist: Brands, Categories, Inventory/Product management (incl. bulk upload).
>
> **REWORK — professional ops panel (D, 2026-07-03..06, branch `feature/admin-panel-rework`):**
> full audit (`docs/ADMIN_PANEL_AUDIT.md`) then 5 phases — security lockdown (admin-only gate +
> `requireAdmin()` in every service-role action), approved cuts (Live Map CUT → dashboard Active
> Deliveries list; Try-analytics merged into Analytics; Brands+Categories → one Taxonomy screen;
> CMS parked out of nav), store-panel design tokens at ops density + grouped sidebar + **global
> ⌘K search**, action-first dashboard (Needs-Attention queues), orders (saved views, bulk
> actions, minutes-correct try column) + order detail (timeline, store/payment links, per-order
> money reconciliation, confirm+reason overrides with cancel cleanup), payout double-payout
> guard (migration **032**, applied), delivery Fail action, route-level loading/error states.
> Dilip browser-tested the lot 2026-07-06. Still open: 2FA (below), refunds/payment-retry
> (Razorpay, partner), server-side pagination (pre-scale).
- [~] 1. Admin Login  *(works; no 2FA yet)*
- [x] 2. Master Dashboard
- [x] 3. All Orders Management
- [x] 4. Order Detail (Admin)
- [x] 5. Customer Management
- [x] 6. Store / Partner Management
- [x] 7. Delivery Agent Management  *(Riders)*
- [x] 8. Revenue & Financial Analytics
- [T] 9. Payment Records — D  *(/admin/payments: ledger over `payments` joined to orders + users; Total Captured / Successful / Failed summary cards; status tabs (success/initiated/pending/failed/refunded) + search; row → order detail. Verified in browser with live data. **2026-07-10: grew its one write action — full Refund on successful Razorpay rows** (confirm-with-reason modal → guarded `refundPayment` server action → Razorpay refund API → row flips to `refunded`, audit-logged; migration **041** pending apply + browser check).)*
- [T] 10. Try & Return Analytics — D  *(**merged into /admin/analytics** as a section during the 2026-07 rework — standalone route removed; same data: session lifecycle, keep-vs-return split, 30-day chart, returns-by-condition.)*
- [x] 11. Live Deliveries Map — **CUT (2026-07 rework, owner-approved)** *(over-engineered for 1–2 riders — it was an address-embed iframe, not live tracking. Replaced by the dashboard **Active Deliveries** list: realtime rows, tap-to-call rider/customer, per-delivery Google-Maps link. No schema cleanup was needed.)*
- [T] 12. Notifications & Alerts Management — D  *(/admin/notifications: compose + send a notification to all users / by role / a single user (email lookup) via guarded `sendNotification` server action (bulk insert + audit-log); history list with type filter + search + read status. No migration — `notifications` table + RLS already exist. Send flow + audit-log entry verified in browser.)*
- [T] 13. Complaints & Support Management — D  *(/admin/complaints: list + status filter + search, manage modal (status + response) via guarded `updateComplaint` (audit-logged). **Migration 012** (complaints table + RLS). Verified in browser. Customer-side submission UI separate.)*
- [x] 14. Discount & Promo Code Manager  *(Coupons)*
- [T] 15. Content Management (CMS) — D  *(/admin/content: list + create/edit/publish/delete content blocks (key/title/body/type/published) via guarded `saveContentBlock`/`deleteContentBlock` (audit-logged). **Migration 013** (content_blocks table + RLS — published rows world-readable for the customer site). Verified in browser.)*
- [T] 16. User Role Management — D  *(/admin/users: list users + role filter + search; change-role modal → guarded `changeUserRole` server action (admin client) that updates `users.role`, provisions `store_managers` (store picker) / `riders`, deactivates store assignments on demotion, blocks self-role-change, warns on granting admin, and audit-logs via `logActivity`. No migration — all roles already in the `user_role` enum. Verified in browser. Agent-panel-specific provisioning deferred to Jay's agent schema.)*
- [T] 17. Store Payout Management — D  *(/admin/payouts: per-store kept revenue (order_items decision=keep via products.store_id) − commission (`system_settings.commission_rate`) = net owed, minus paid `payouts` = outstanding; "Record payout" inserts per-order `payouts` rows (status=paid), audit-logged. No migration. **Actual Razorpay disbursement stubbed** (shared infra, partner). Verified in browser.)*
- [~] 18. Agent Payout Management — D  *(/admin/agent-payouts: per-rider earned = Σ `orders.delivery_fee` over completed deliveries, minus paid `agent_payouts` = outstanding; "Record payout" inserts per-order `agent_payouts` rows (status=paid), audit-logged via `logActivity`. Mirrors #17 Store Payouts. **Migration 020** (agent_payouts table + RLS). **Actual Razorpay disbursement stubbed** (shared infra, partner). **Built — needs migration 020 + a logged-in browser check.**)*
- [T] 19. System Settings — D  *(real: `system_settings` singleton table + RLS (authenticated read / admin write, migration 011) + getSettings/updateSettings server actions; persists commission rate + try-window (stored in **minutes**) + general/delivery fields; replaces the mock toast. Migration applied to the shared DB; save verified in a real browser.)*
- [T] 20. Reports & Export Center — D  *(/admin/reports: client-side CSV export of orders, payments, customers, products, payouts (Blob download); each export audit-logged. No migration. Verified in browser.)*
- [T] 21. Admin Activity Log — D  *(/admin/activity: read-only audit over `activity_logs` (admin join, latest 200) with entity-type filter, search, expandable before/after JSON diff. Plus a reusable `lib/activity.ts` `logActivity()` helper wired into ALL admin mutations (products, orders, customers, riders, stores, brands, categories, coupons, deliveries). No migration. Verified in browser.)*

---

## 🚀 LAUNCH PLAN TASK BOARD (2026-07-14 — soft launch target ~6 weeks)
> Full detail, gap evidence, dependencies and rationale: **`docs/FITZO-LAUNCH-PLAN.pdf`** (12 pages).
> Task IDs are stable — use them in branch names/PR titles (e.g. `feat/customer-address-A1`).
> Decisions locked: soft launch 4–6 wks · no entity yet (registration = Week 1) · Netlify ·
> AI skin-tone CUT · slots + coupons KEPT (lean) · **single-store cart v1 (G1)**.
> Rules unchanged: feature branches; migrations idempotent + numbered sequentially — **claim the
> next free number here before writing one** (043–046 reserved for Dilip's money steps M1–M4);
> each task ends with typecheck + builds + a PROGRESS entry (`/finish-task`).

**Dilip (money / infra / admin / DB):**
- [~] W1.6 **M1 gateway-fee capture** (mig 043): payments.gateway_fee/tax stamped by webhook, admin backfill sync — **built (branch `feat/launch-plan-money-m1`), migration 043 pending apply + browser check**
- [~] W1.7+W2.7 **M2 COMPLETE — `order_economics` (044, applied) + `store_order_economics` (045, pending apply)**: Money card, Store Payouts, store Earnings, dashboard revenue, analytics revenue all read the one refund-aware money truth. Dashboard/analytics browser-verified; payouts/earnings RPC path live once 045 is applied
- [~] W2.1 **Supabase prod/dev split** — **prod project `fitzo-prod` (ref bozqclrtbxkjevgztruc, Mumbai) CREATED + schema applied + Vault secrets done (2026-07-15).** Bootstrapped from `packages/supabase/baselines/prod_bootstrap.sql` (schema.sql + migrations 043-and-prior through 047, one transaction) — REST smoke-check confirms every table, `order_economics`, and all money/logic RPCs resolve. Repo side: `docs/ENVIRONMENTS.md` runbook + `scripts/supabase/{dump-baseline,verify-env}.sh`. **Remaining prod steps: Part D auth (redirect URLs + phone-off now; SMTP waits on B6/W2.4 provider), Part F backups toggle, Part G `verify-env.sh` full RLS audit (needs psql + prod conn — do before go-live W5), first admin user (after apps point at prod). Env matrix / Netlify = W2.2 (Jay).**
- [~] W2.8 **G3 stock reservation** — **built (branch `feat/infra-env-split-w21`), migration 047 pending apply + browser check.** `place_order()` RPC = atomic placement (stock reserved under row locks **+ G2 server-side pricing + G1 single-store backstop included** — ⚠️ Jay: your W1.4/W2.6 server halves are covered, client UX remains yours); releases ride triggers on decision/status transitions (keep/return/cancel — all existing sweeps covered without edits)
- [ ] W2.9 Backups/PITR + pg_cron sweeps (027+036) on prod (B8)
- [~] W3.4 **M3 commission stamped at settlement** (mig **046**) + per-store override — **built (branch `feat/money-m3-commission-046`), 046 pending apply + browser check** *(pulled forward from Week 3; PR #39 landed M1+M2 early)*
- [ ] W3.5 **D1 admin MFA (TOTP) + email allowlist**
- [~] W3.6 **G9 delivery-fee policy rework — built end-to-end (branch `feat/money-g9-delivery-fee-050`, migration 050 pending apply + live-fire).** Upfront fee payment (own Razorpay charge, tracking-page pay card), `first_order_free` toggle in admin Settings, kept-value auto-refund via `record_delivery_fee_refund` RPC + `refundDeliveryFeeIfEligible` action, store-confirm gate (`DELIVERY_FEE_UNPAID` → friendly store copy), checkout copy aligned. Closes G8. **D built the customer UI wiring too — Jay: only polish left (checkout-inline fee payment if wanted; the tracking-page card is the functional path)**
- [ ] W4.1 **D3 RLS probe suite in CI** (anon + cross-tenant probes — the 2026-06-08 incident, automated)
- [ ] W4.4 Auth settings pass on both projects (D4) · W4.9 rotate leaked Razorpay test secret (C4)
- [~] W4.7 **G6 store serviceability + Open/Paused — built (branch `feat/store-pause-serviceability-w47`, migration 052 pending apply).** (1) Admin approval now checks the STORE's pincode against the shared Pune list (new **`@fitzo/pincode`** package — the customer app re-exports it, so the two can't drift); outside-area/no-pincode stores need an explicit audit-logged "Approve anyway" override. (2) Store dashboard gains an Open/Paused card → `store_set_paused` RPC; paused stores stay visible but `place_order` raises `STORE_PAUSED` (friendly checkout copy). **Dilip: apply 052 + the browser steps in Known-issues.**
- [ ] W4.8 **M4 tax provisions** (mig 046, toggles OFF until GST) — needs CA input
- [ ] W5.2 **Razorpay LIVE cutover** (gated on KYC C3) · W5.5 ops runbook · W6.2 M5 P&L screen
- [ ] Business chase (with J): CA call + entity filing **Week 1**, GST, Razorpay live KYC

**Jay (customer / agent / growth surface):**
- [~] W1.3 **A1 address at checkout**: real address form → `addresses` row → stamped on order (rider drop card + admin already read it) — **built + browser-verified live (branch `feat/customer-address-A1`; order FTZ-2026-00053 carries a full address_id; add-form/quick-switch/add-new all driven in preview)**
- [~] W1.4 **G2 server-side pricing (CRITICAL, D reviews PR)**: placeOrder resolves price from DB, ignores client `priceValue`; validates is_active/not-deleted/store-approved — **built + tamper-tested live (branch `feat/customer-pricing-G2`, stacked on A1): cart price forced to ₹1 via localStorage → DB row still ₹909/₹958. Awaiting Dilip's review**
- [~] W1.5 **A3 try-window single source of truth**: everything reads `system_settings.try_window_minutes`; hardcoded 7s deleted; owner decided the window IS 7 min (60 was the delivery promise) — **built + verified reading the live setting (checkout deadline came out started_at+60min = current live value; branch `feat/customer-trywindow-A3`, stacked on G2), migration 048 pending apply (flips the value to 7 + fixes the RPC)**
- [ ] W1.2 SMS/DLT registration start (MSG91) + SMTP provider signup + fitzo.in domain verify (B6/B7/C6)
- [ ] W2.2 **Netlify 4 sites** + staging domains + per-app env vars (service-role ONLY on admin) (B3)
- [~] W2.3 **GitHub Actions CI** — **built + CI green on a real GH run (PR #45, `chore/ci-github-actions`).** `.github/workflows/ci.yml`: on push-to-main + PR-to-main, two jobs — `check` (typecheck ×4 + vitest, 39 tests) and `build` (4-app matrix, `next build` with PLACEHOLDER Supabase/Razorpay env so no repo secrets needed). pnpm 9.15.0 + Node 22, `--frozen-lockfile`. **lint deliberately EXCLUDED** — no app has an ESLint config so `next lint` prompts interactively (would hang CI); follow-up: configure ESLint then add a lint job. **Remaining: merge #45, then enable branch protection on `main` (require the 5 checks) — that's a GitHub-settings step (Jay/Dilip), not code.** (J)
- [ ] W2.4 Custom SMTP live on both Supabase projects (B6) · W2.5 Sentry × 4 apps + uptime (B5)
- [~] W2.6 **G1 single-store cart**: "Replace bag?" prompt + placeOrder rejects mixed-store carts server-side — **built + browser-verified (branch `feat/customer-single-store-cart-G1`, stacked on A3): conflict modal (start-new-bag / checkout-first / cancel) + server reject of a hand-tampered mixed cart both driven live in preview. Pulled forward from W2 (2026-07-15, owner picked it as next).**
- [ ] W3.1 **A2 lean slot booking**: date + 2–3 windows at checkout → shown on store card, rider offer/detail, admin orders (needs a small migration — claim next number)
- [ ] W3.2 **A4 lean coupons**: validate against admin coupons table, stamp into orders.discount columns (commission-base rule agreed with D / M3)
- [ ] W3.3 **A5+G7 copy sweep**: COD out of footer, Pay-Later/wallet hidden, AI placeholders removed, fake-reviews page parked, how-it-works updated (slots + one-shop carts)
- [ ] W4.2 **A6 transactional emails** (confirmed / on-the-way / receipt / refund) via SMTP provider
- [ ] W4.3 Playwright E2E happy path vs staging · W4.5 **G4 customer cancel** (RPC frees stock)
- [~] W4.6 **G5 abuse caps — built by D (branch `feat/abuse-caps-g5-053`, stacked on 052; migration 053 pending apply).** Three settings-driven caps enforced inside `place_order` (0 = off): max items/order (default 8), max active orders (default **1**, the plan's number), daily cap (default OFF). Admin Settings gains an "Order Limits" section; checkout maps the three error codes to friendly copy. **Dilip: apply 053 + browser steps in Known-issues.**
- [ ] W5.1 domains live · W5.3 seed real stores/riders (with D) · W5.4 live dry-run (with D)

**Bump order if a week runs hot:** coupons (W3.2) → abuse caps (W4.6) → store pause (W4.7). **Never bump G1/G2/G3 — correctness, not features.**

## Decisions log (append-only — record anything non-obvious you decided)
- 2026-07-17 (later): **G5/W4.6 — abuse caps built** (branch `feat/abuse-caps-g5-053` STACKED on the 052 branch, **migration 053**, pending apply; M4 tax → 054). D took this Jay-column task on owner request — it's placeOrder-RPC work, the server half D already owns via 047/052. Exactly the plan's v1 (config caps; deposits/holds stay post-launch): (1) **the caps live at the top of `place_order`**, before any product/stock work — server-side is the only enforcement that survives a tampered client, and fail-fast means an over-cap order costs two COUNT queries, not row locks; (2) **053's function body = 052's + the caps block** — function recreations don't compose, so the branch stacks on G6's (merge/apply order matters and is stated in-file); (3) semantics chosen for FAIRNESS to legit customers: "active" = not completed/cancelled (a return-in-flight still blocks — the rider trip is real), but the daily cap EXCLUDES cancelled orders because today cancellation is admin/expiry-driven (store never confirmed ≠ customer's fault) — flagged in-file to revisit when G4 self-cancel lands; (4) errors CARRY the limit (`ORDER_LIMIT_ACTIVE:1`) so checkout copy says the number and singular/plural correctly; (5) defaults per the plan — 8 items, **1 active order**, daily OFF — all admin-tunable (Settings → Order Limits, pre-053 degrade errors cleanly without touching other sections' saves). ⚠️ apply-day note: existing test accounts with stale active orders will immediately hit the active-order cap — that's the feature, not a bug. Verified: typecheck + admin/customer builds; Settings section rendered via throwaway preview route (deleted); cap logic itself needs 053 applied — Dilip's browser steps in Known-issues.
- 2026-07-17: **G6/W4.7 — store serviceability + Open/Paused built** (branch `feat/store-pause-serviceability-w47`, **migration 052**, pending apply; M4 tax → 053; 051 = coupons lockdown on PR #47). Design: (1) **pause ≠ deactivate** — `is_paused` is a NEW manager-controlled column, flipped only via the guarded `store_set_paused` RPC (008 pattern), because reusing `is_active` would let a manager un-do an admin deactivation; (2) **pause blocks PLACEMENT, not visibility** — the store + catalogue stay browsable (Swiggy-closed pattern), `place_order` raises `STORE_PAUSED:<name>` mapped to "temporarily closed" at checkout, and everything already placed keeps flowing (the store still owes fulfilment); dashboards hide the toggle entirely on a pre-052 DB (no dead switch); (3) **the pincode list got hoisted to `@fitzo/pincode`** (new workspace package, transpiled source like @fitzo/supabase) because the admin approval gate must use the SAME Pune list the customer app checks — a copied list WILL drift and then admin approves stores the customer app calls unserviceable; customer's `lib/pincode.ts` re-exports it (zero import churn) and keeps the ETA/messaging helpers; (4) **approval is a soft gate with an audited override** — outside-area (or missing) pincode warns and demands a second "Approve anyway" click that stamps `serviceability_override: true` + the pincode into the activity log, rather than a hard block: admins are the override authority (coverage may genuinely be expanding), but a silent approve of an undeliverable store is the G6 bug. Store hours are CUT (lean) — pause covers the need. Verified: typecheck ×4, customer+admin+store builds (CI placeholder env), 23 store + 16 customer tests, and a live preview pass of the customer landing pincode flow through the new package (110001 → "only in Pune" ✗, 411001 → accepted). Logged-in pause/approve flows need 052 — Dilip's browser steps in Known-issues. (owner request after the 050 live-fire — "Swiggy-style bill"):** (1) tapping **Keep** now opens a **bill sheet BEFORE Razorpay** — item price / delivery-fee line ("already paid — not charged again" · "+ ₹49 added to this payment" on the legacy 040 path · "refunded — free delivery") / bold "To pay now" — using the SERVER-computed amounts from createKeepPayment, so the sheet always equals the actual charge; cancel leaves the initiated payments row unconsumed (same as dismissing the modal). (2) The tracking page's amount summary became a full **Bill details card**: per-item rows with keeping/returned/deciding states (returned = struck through), items total, delivery-fee row in all five states (free / to-pay / paid / refunded / with-first-Keep), and "Paid so far" = Σ successful captures (page now makes ONE payments fetch driving both fee state and the bill). typecheck + customer build pass; visual check is Dilip's (open any live order → bill card; tap Keep → sheet).
- 2026-07-16: **G9/W3.6 — delivery fee upfront, built end-to-end** (branch `feat/money-g9-delivery-fee-050`, **migration 050**, pending apply; M4 tax → 051). Implementation of the 2026-07-14 policy decision, leaning hard on composition: the upfront payment is just a payments row with `delivery_fee_component` set, so **040's carried-check, the 039 webhook settle, 041's refund columns and 044/046's refund-aware economics all work unmodified**. Design calls: (1) **fee settles through the SAME verified path as keeps** (confirm_keep_payment HMAC → settle_keep_payment) — no new settle plumbing; the one change is that the order-paid rollup is now gated on `order_item_id IS NOT NULL` (a fee settle on a fresh order must not mark it 'paid'); (2) **enforcement lives in `store_confirm_order`** (`DELIVERY_FEE_UNPAID`), not checkout — a fee-dodger who closes the modal keeps an order that simply never moves, and 036's sweep + 047's stock-release trigger clean it up on the existing clocks; (3) **auto-refund is app-then-RPC**: Razorpay refund API first (customer app holds keys server-side), then `record_delivery_fee_refund` flips the ledger after re-verifying in-DB (owner + ALL-decided — never mid-window, else a later Keep re-charges the fee via 040 — + kept-paid ≥ threshold + standalone fee payment only; legacy 040-folded fees stay manual via admin 041, full-refund-only). Trust note recorded in-file: a forged RPC call without a real refund only mis-marks the caller's own row (no money moves); M6 reconciliation is the backstop. (4) Refund fires from confirm/return actions post-finalize + a **tracking-page catch-up effect** for rider-completed (auto-return) orders — self-heal pattern like expire_order_if_due; admin 041 refund is the ultimate backstop. (5) Checkout display: 050-probed — shows "refunded when you keep ₹X+" / "first order free 🎉"; pre-050 keeps the legacy subtotal-waiver display so screen always matches what the RPC actually stamps. Degrades everywhere (probe-gated); typecheck ×4 + customer/admin/store builds + 23 tests pass. **Dilip: apply 050, then the 5-step live-fire in Known-issues.**
- 2026-07-16: **G3 place_order reconciled with A1 addresses (migration 049)** — the launch-prep branch (`feat/infra-env-split-w21`, 046/047) was cut before the A1→G2→A3→G1 stack merged to main, so its `place_order()` RPC created orders with `address_id = NULL` (rider drop card + admin order detail would lose the address) and its `checkout/actions.ts` was a 2-arg version that dropped `addressId`. Reconciled by merging main into the branch and: **(1) new migration 049** (not an edit to the already-applied 047 — avoids the file-vs-DB drift the ENVIRONMENTS runbook warns about) drops the 2-arg `place_order` and recreates it as `place_order(p_items, p_payment_method, p_address_id)` — requires the address, re-checks it belongs to `auth.uid()` (mandatory: the RPC is SECURITY DEFINER so it must not stamp an arbitrary user's address), and stamps `orders.address_id` so the 014 delivery trigger snapshots the drop address exactly as A1 established; **(2) `actions.ts`** keeps main's address+G2 path as the legacy fallback and passes `p_address_id` to the RPC. Pincode serviceability stays in the app layer (`lib/pincode.ts` single source of truth) — the RPC owns ownership+atomicity, not the Pune list. `friendlyOrderError` gained ADDRESS_REQUIRED/ADDRESS_INVALID. ⚠️ **049 pending apply on dev + prod** (048 try-window too, if not already). Re-verify the order path on dev before this merges.
- 2026-07-15: **Prod project bootstrapped (W2.1).** Chose **replay-from-repo over dump-dev** for the fresh prod: `schema.sql` turned out to be the v0 base (none of the migration additions present), so repo = schema.sql + migrations, and replaying it needs NO DB passwords (the dump path needs the dev DB password) AND is literally reproducible-from-repo (the B2 goal) AND carries the auth trigger / realtime publication / storage bucket that a public-schema dump omits. Generated `packages/supabase/baselines/prod_bootstrap.sql`: schema.sql (postgis stripped — unused) + all migrations in order + the system_settings seed, **wrapped in ONE transaction** so a fresh apply is atomic (schema.sql alone isn't idempotent, so the txn is what makes fix-and-re-run safe). Pre-flight scan confirmed no txn-hazard statements (no enum ADD VALUE / CONCURRENTLY; pg_cron + vault calls are comment-only). **Snag caught pre-apply: migration 046 lives on the un-merged `feat/money-m3-commission-046` branch, so the infra branch's bootstrap was missing it** (would've given prod the pre-stamping settle_keep_payment + views) — pulled the 046 file onto the infra branch to complete it (harmless identical-file dup across branches until they merge). Dilip applied it clean + set Vault secrets; REST smoke-check green. ⚠️ **Branch hygiene TODO: 046 (M3) + 047/W2.1 (infra) still need to reach main — per grouped-PR pref, bundle M3+G3+infra into one launch-prep PR.**
- 2026-07-14: **G3/W2.8 — stock reservation built** (branch `feat/infra-env-split-w21`, **migration 047**, pending apply; M4 tax → 048). The schema had the textbook model waiting since day one (`available_qty` is GENERATED stock−reserved) — nothing ever wrote it. Design: (1) **one atomic `place_order()` RPC** instead of the client-insert sequence, because stock check + price resolution + order creation must share a transaction to be correct — this deliberately absorbs the SERVER halves of Jay's G2 (prices from DB; client `priceValue` ignored) and G1 (`MULTI_STORE_CART` error): they're ~10 lines each inside the RPC and leaving them out would ship a new money-critical entry point with known holes. **Jay's remaining halves are client UX** (replace-bag prompt, friendly error surfaces — `friendlyOrderError` in checkout/actions.ts already maps the codes). (2) **Releases are transition TRIGGERS, not code-path edits**: pending→keep consumes (stock−1, reserved−1), pending→return releases (reserved−1), order→cancelled bulk-releases — so settle_keep_payment (046), returnItem, auto_return (027), rider_fail (033), expire_stale_offers (036) and admin Cancel all restore stock without touching any of them; idempotency via `order_items.stock_reserved` (cancel's release UPDATE doesn't SET `decision`, so the item trigger can't double-fire — trigger is `BEFORE UPDATE OF decision`). (3) Explicitly-chosen out-of-stock size ERRORS instead of the old silent any-variant substitution; variants lock `FOR UPDATE` in id order (no deadlocks). (4) placeOrder falls back to the legacy path ONLY on PGRST202 (function missing), so pre-047 behavior is byte-identical. typecheck ×4 + customer build pass; live-fire (place → keep → return → cancel stock movements + 2-store cart error) needs 047 applied — Dilip's browser step (steps in Known-issues).
- 2026-07-14: **W2.1 — prod/dev split, repo half built** (branch `feat/infra-env-split-w21`; creating the actual `fitzo-prod` project needs the Supabase dashboard, so the deliverable is a runbook + scripts that make Dilip's half a 30–45 min checklist). Key calls: (1) **prod's baseline = a schema-only dump of the LIVE dev DB**, not a replay of the 46 migration files — dev is the truth and hand-applied SQL can drift from the repo (the 2026-06-08 RLS incident is exactly that class); `scripts/supabase/dump-baseline.sh` writes it to `supabase/baselines/` (committed = prod's birth certificate). (2) The runbook (**`docs/ENVIRONMENTS.md`**) explicitly lists what a public-schema dump does NOT carry — the `auth.users` trigger (029), realtime publication membership (018/021), the storage bucket (030, bucket rows are data), pg_cron schedules (027/036), Vault secrets, and the `system_settings` seed row — each with the re-run step. (3) `scripts/supabase/verify-env.sh` turns "did we set it up right?" into one command run against BOTH envs: fails on any public table with RLS off, missing money functions/view, missing auth trigger/bucket/publication rows, wrong replica identity. (4) **Migration workflow after the split**: `packages/supabase/migrations/NNN_*.sql` stays the single migrations home; dev first → verify → merge → prod applies only MERGED migrations; test fixtures never leave dev. Scripts syntax-checked; live runs need DB passwords (not in repo) — Dilip's step per the runbook.
- 2026-07-14: **Money M3 — commission stamped at settlement + per-store override built** (branch `feat/money-m3-commission-046`, **migration 046**, pending apply; M1+M2 merged to main via **PR #39** the same day, 043–045 all applied). Design: (1) stamping lives in `settle_keep_payment` — the ONE place both payment paths (client confirm + webhook) flip an item to keep, so the freeze can't be bypassed; fill-only, so races/re-settles never overwrite; (2) rate resolution = `stores.commission_rate` (new, NULL = default) → `system_settings.commission_rate` → 15, resolved per ITEM via its product's store; (3) **backfill stamps all existing kept-and-paid items at today's effective rates** — deliberately identical rupees to what every screen already shows, so applying 046 changes nothing visible, it only makes history immutable; (4) both economics readers (044 view + 045 RPC) recreated to Σ stamped amounts, keeping a store-aware live-rate fallback ONLY for the settled-unstamped edge case; view's `commission_rate` column stays the platform default and the Money card now derives its displayed % from amounts (blended truth on mixed-rate orders); (5) admin UI is a "Commission override (%)" field on Store detail's existing Store-Info form (admin-RLS direct update, audit-logged; rate saved as a second update so a pre-046 DB still saves the rest of the form and errors the rate cleanly). Verified: typecheck ×4, admin+store builds, 23 store tests, field renders on Little Stars store detail in the logged-in preview. **Dilip: apply 046 → all money screens should show the SAME rupees; then set an override on a test store, settle a new keep, confirm it stamps the override rate while old orders stay put; then change the Settings rate and confirm historical payables don't move.**
- 2026-07-15 (later): **G1 single-store cart — built, pulled forward from W2.6** (branch `feat/customer-single-store-cart-G1`, stacked on the A1→G2→A3 trio because it also touches `checkout/actions.ts`; J, owner picked it while waiting on Dilip's G2 review). Why: a mixed-store bag is physically undeliverable (one rider, one pickup store — 033 stamps only the FIRST store's pickup_address) and would break the 60-min delivery + doorstep try-on. Design: **(1) cart items now carry `storeId`/`storeName`** — threaded from the already-fetched `stores(id, name)` through FrontendProduct/ProductDetailData → AddToBag/ProductInfo/catalogue-card wishlist items → WishlistItem → CartItem (all optional: pre-G1 persisted carts lack them, the server guard is the hard stop); **(2) pure `findStoreConflict()` in `lib/storeConflict.ts`** (+6 vitest) powers the add-time check inside CartProvider.addItem, which parks the refused item and opens the new **StoreConflictModal** ("One order, one store" — primary *Start a new bag with {B}* replaces the bag with the refused item, secondary *Checkout my {A} bag first* closes-then-routes to /checkout (provider-mounted-modal lesson), tertiary cancel; transform-only entrance per the 2026-07-10 animation lesson, reduced-motion respected); addItem's existing `false` return contract means callers already skip their "Added ✓" side effects; **(3) server guard in placeOrder**: distinct `store_id`s across the bag → friendly reject (uses the G2 product fetch, zero extra queries). **Browser-verified live**: Little Stars tee in bag → adding Denim Depot jeans popped the modal (screenshot in PR #43); start-new-bag swapped the bag; checkout-first landed on /checkout with bag intact + no lingering overlay; **hand-injected mixed cart via localStorage → server refused with the one-store message, no order row created**. typecheck + 16 vitest + customer build pass. Recommended-products rails (mock "FitZo Select" items) still add store-less wishlist entries — harmless, server guard covers; unify when those rails go live-data. (J; A1 → G2 → A3, each branch cut from the previous because all three touch `apps/customer/app/checkout/actions.ts`; merge in order). **(1) A1 address at checkout** (`feat/customer-address-A1`): Blinkit-style block replaces the static pincode text — saved default address card, quick-switch list, inline add-new form (name/phone/house/street/landmark/city/pincode, Home/Work/Other label) writing to the existing `addresses` table via new `lib/addresses.ts` (+ 7 vitest tests on validation/formatting); `placeOrder` now REQUIRES an address_id, re-verifies ownership (RLS) + the Pune pincode gate server-side, and stamps `orders.address_id` — migration 014's `create_delivery_for_order` trigger then snapshots the full address into `deliveries.drop_address`, so the rider drop card + admin order detail get real addresses with zero further work. Also killed the **fake flat ₹300 checkout "discount"** (display-only, never charged — totals now match the server). Profile's address book (own UI) still saves without phone/state — checkout-created addresses are complete; unifying profile's form onto `lib/addresses.ts` is a nice-to-have follow-up. **(2) G2 server-side pricing** (`feat/customer-pricing-G2`, **money-critical — Dilip reviews before merge**): `placeOrder` resolves every price from the DB (`discounted_price ?? base_price`), ignores client `priceValue`/`title` in all money math (`price_at_order`, `subtotal`, `product_name`), rejects items whose product is RLS-invisible (inactive/deleted), whose store isn't approved+active (checked via the `stores` join), whose price is non-positive (a malformed bulk-upload row can't sell for ₹0), or whose quantity isn't a small positive integer (blocks negative-qty tampering; real caps = G5). This secures the whole chain because `createKeepPayment` charges `price_at_order`. Compatible with admin bulk-upload data by construction (it stamps base_price/is_active/is_deleted). **(3) A3 try-window** (`feat/customer-trywindow-A3`, **migration 048** pending apply): owner decision — the window IS **7 minutes**; the live setting's 60 was a mix-up with the 60-minute delivery promise, so 048 corrects it (guarded 60→7) and re-creates `start_try_window()` to read the setting instead of its hardcoded 7. App-side: checkout's placeholder deadline + tracking timeline/doorstep-prompt copy + confirmation copy all read `try_window_minutes` (fallback 7). Static marketing copy (how-it-works/about/terms) says "7 minutes" and now matches the decided value — left as copy. typecheck + 10 vitest + customer build pass on the stack tip. **Browser-verified live (preview, logged-in)**: fresh signup → add-form auto-opens on empty account → saved → order **FTZ-2026-00053** stamped with the full address row; second address saved → quick-switch radio list + Change verified; **G2 tamper test: cart priceValue forced to ₹1 in localStorage → DB shows price_at_order 909 / subtotal 909 / final_amount 958** (server price won); try_session deadline = started_at + 60 min → the LIVE setting is being read (48 flips it to 7). **Test data to clean:** account `uitest-a1@fitzo.dev` (2 addresses) + order FTZ-2026-00053 (pending — store never confirms it). **Next: Dilip reviews G2, applies 048.**
- 2026-07-14: **Money M2 part 2 (W2.7) — every money consumer repointed to the one truth** (branch `feat/launch-plan-money-m1`, **migration 045**, pending apply; 044 applied + numbers checked by Dilip same day). Key design call: the per-store slice is a **guarded SECURITY DEFINER RPC (`store_order_economics`)**, not a second security_invoker view — store managers can't SELECT customer `payments` under RLS, so an invoker view would silently report every kept item as unpaid for exactly the panel that needs it; the RPC bypasses RLS internally and gates explicitly (NULL→admin-only all-stores; store id→manager-of or admin), the same pattern as 031. Repointed: `computeStorePayables` (+ its record-payout action → **a refunded order stops being owed to the store automatically**), store `loadPayoutSummary` (new `excludedGross` + breakdown line, only rendered when non-zero), dashboard Today's-Revenue + weekly chart, analytics 30-day revenue (both now `net_captured`, subtitled "Captured − refunds"). Agent payouts deliberately untouched (flat rider_fee on completed deliveries is already refund-independent and equals the view's rider_cost). Every consumer keeps a pre-045 legacy fallback. **Renumbering: M3→046, M4→047** (045 was reserved for M3; PDF plan not regenerated, PROGRESS is the tracker). Verified: typecheck all 4 apps, admin+store builds, 23 store tests, and a live logged-in browser pass — dashboard Today's Revenue ₹958 "Captured − refunds" (= FTZ-2026-00052's capture), analytics 30-day ₹7,553 = Payments' Total Captured exactly (old basis: Σ final_amount over 40 orders, wildly inflated), store-payables fallback path exercised by the dashboard owed-queue. **Dilip: apply 045, then eyeball Store Payouts + store Earnings — same rupees as before (no refunds in the data yet), now refund-proof.**
- 2026-07-14: **Money M2 part 1 — `order_economics` view + Money-card repoint built** (branch `feat/launch-plan-money-m1`, **migration 044**, pending apply; 043 applied + fees synced by Dilip the same day — verified live: every captured Razorpay row carries exactly 2% fee, e.g. ₹19.16 on ₹958; initiated rows correctly NULL). Design decisions: (1) **revenue keys on PAYMENTS, not item decisions** (the "accounting-only" refund semantics from the plan): `kept_paid_gross` counts a kept item only while it has a live SUCCESS payment, so a refund reverses commission/store_net/margin with no item-state rewrite; (2) `gateway_cost` includes fees on REFUNDED rows (Razorpay keeps its fee — sunk cost) + a `gateway_cost_incomplete` flag when captured rows lack fee data (Money card shows "partial" → run Sync); (3) **rider cost gated on delivery COMPLETED** — margin never books unearned rider pay (visible on FTZ-2026-00052: delivery sits at 'arrived', so margin 166.19 not 126.19 until the rider completes); (4) `store_net = kept_paid_gross − commission` (additive complement — the Money card's existing formula; ≤1p from the payout compute's independent rounding, which W2.7 kills anyway); (5) **plain view with `security_invoker`** — caller's RLS applies (admin sees all, customer at most their own rows), no SECURITY DEFINER, no RLS widening; cfg is a scalar subquery so a missing settings singleton can't blank the view; (6) commission still reads the live settings rate — M3 (045) stamps it at settlement and the view switches to stamped values. Money card degrades pre-044 to the old inline math (no refund/gateway lines) with a footnote saying to apply 044. typecheck + admin build pass. **Dilip: apply 044, open FTZ-2026-00052 → expect captured 958 / collected 49 / gateway −19.16 / commission 136.35 / store net 772.65 / margin 166.19.**
- 2026-07-14: **Money M1 — gateway-fee capture built** (branch `feat/launch-plan-money-m1`, **migration 043**, pending apply). Every margin figure ignored Razorpay's ~2% MDR + 18% GST; Razorpay reports the exact per-payment numbers, so nothing is estimated. Design: (1) 043 adds `payments.gateway_fee`/`gateway_tax` (rupees) with the semantics documented in-file (fee INCLUDES tax; net MDR = fee − tax; fees NOT returned on refund → kept on refunded rows as sunk cost; NULL = not-yet-known vs 0 = genuinely free); (2) `razorpay_webhook_captured` re-created verbatim from 039 + a **fill-only fee stamp placed BEFORE the settle branch** — deliberate, because the client path usually wins the settle race and returns `already_settled`, and the client success handler never sees fee data, so the webhook is the only automatic source; (3) client confirm path deliberately NOT changed (customer app has only the anon key — a fee write would need a new RPC for data the webhook + backfill already provide); (4) admin backfill `syncGatewayFees` (requireAdmin → 043-column pre-flight BEFORE any Razorpay call → Payments fetch API in batches of 50 → fill-only updates, audit-logged) behind a "Sync gateway fees" button on Payment Records, which also shows a muted "fee −₹X" line under Amount. Pre-043 degrade: page selects the column separately so it renders without it; sync errors cleanly. **Dilip's steps: apply 043, click Sync (covers the 10 existing rows), and after the 039 webhook is registered new captures stamp themselves.**
- 2026-07-14 (later): **G9 — delivery-fee collection logic is a guaranteed loss on return-everything orders; policy decided (owner: Dilip).** Today the fee rides the FIRST Keep charge (040), so a customer who returns everything pays ₹0 while Fitzo pays the rider ₹40 + store handling — pure loss, stacking with G5 (no order caps). Decision: **fee becomes its own upfront Razorpay payment at checkout** (collected regardless of keep/return, doubles as commitment against abuse) with two config waivers in `system_settings`: **first_order_free** (acquisition) and **kept-value waiver** — `free_delivery_above` re-based from ORDERED to KEPT value, evaluated at settle; crossing it auto-refunds the fee payment (reuses the 041 refund path on the standalone fee payment; no partial-refund work needed). Composes with existing code: 040's "fee already carried by a successful payment?" check sees the upfront payment and stops folding the fee into the first Keep automatically. This closes G8 (ordered-vs-kept waiver loophole) as a side effect. Rejected alternative: billing the fee at window-end when everything returns — unenforceable in-app. Scheduled as **W3.6** (D money path + J checkout UI, after M2). Trade-off accepted: checkout gains one small payment step where today there is none — that friction is partly the point.
- 2026-07-14: **Launch-readiness audit → 6-week soft-launch plan + task board (D+J split)** — full plan in **`docs/FITZO-LAUNCH-PLAN.pdf`**, task board added above (replaces the stale "Recommended next tasks"). Two audits ran: (1) **money-model audit** — findings: gateway fees (~2%+GST) untracked so every margin overstates; "revenue" on dashboard/analytics is checkout-GMV incl. returned items (truth = captured−refunded); refunds reverse cash but not commission/payouts/margin; zero tax modelling (GST on commission/TCS/TDS-194-O); commission read live from settings (rate change rewrites history — must be stamped at settlement); money math triplicated. Fix = money steps **M1–M6** (migrations **043–046 reserved**: gateway-fee capture → `order_economics` view → commission stamping → tax provisions; then P&L screen, settlement recon post-launch). (2) **business-logic flaw audit ("not vibe-coded"), all verified in code**: **G1** multi-store orders operationally impossible (one delivery, pickup_address = FIRST store only; store B items undeliverable yet keepable; pickup gate can jam the whole order) → **decision: single-store cart v1** (Swiggy pattern, replace-bag prompt + server-side reject); **G2 CRITICAL — placeOrder trusts the client's price** (`price_at_order = item.priceValue` from the browser; Keep charge later trusts that row → tampered request buys ₹5000 item for ₹1) → resolve prices server-side; **G3** no stock check/decrement anywhere (overselling; fallback picks out-of-stock variants) → reservation RPC; **G4** customer can't cancel; **G5** zero-commitment checkout + no order caps = free abuse loop; **G6** stores have no hours/pause + serviceability only checks the CUSTOMER pincode (store can onboard from anywhere); **G7** /reviews shows mockData testimonials labeled "real reactions" → park page; **G8** free-delivery threshold judged on ordered (not kept) value — ACCEPTED for v1, revisit with keep-rate data. Locked decisions: soft launch 4–6 wks; no entity yet (CA/entity filing is a Week-1 task — Razorpay live KYC is the critical path); Netlify hosting; AI skin-tone CUT; slots+coupons kept lean. Open decisions for M2/M4: refund semantics (rec: accounting-only) + tax posture (rec: provision-only until GST reg).
- 2026-07-12: **Track A full E2E in the browser (migrations 037–042 all confirmed applied to the shared DB).** Drove the whole money path on a fresh order FTZ-2026-00051 (2× ₹399 test tees, sub-₹999 so the ₹49 fee applies): checkout stamped delivery_fee=49 + rider_fee=40; store one-tap confirm; rider (Ravi) claimed via the offer card showing **+₹40** (038 rider-pay, not the customer fee); OTP handover; **try-window fee note "The ₹49 delivery fee is added to your first Keep payment" showed (040)**; first Keep charged **₹448 (399+49)** with the Razorpay modal reading "(incl. ₹49 delivery fee)"; payment row settled with **delivery_fee_component=49**; window expired → 2nd item auto-returned → order completed + paid; **admin Money card correct (Task 1): rider pay ₹40 from rider_fee, delivery charge ₹49 line, margin = 60 commission + 49 − 40 = ₹69**. Payouts (042): recorded rider ₹40 + store ₹339.15, both ledger rows carry **reference (UTR) + paid_to snapshot**, both audit-logged with the new fields. Webhook (039): signed payment.captured → in-DB path reached, correctly errors "webhook secret not configured" (Vault pending — Dilip's step); bad/no signature → 401. **Refund (Task 4) — code path fully verified, but the actual refund is BLOCKED on the Razorpay test account:** the guarded action ran end-to-end (auth → 041-column pre-flight → Razorpay refund API) and surfaced "Razorpay: invalid request sent" cleanly in the modal, **correctly leaving the payment row `success` (no false refunded write)**. Reproduced the same `BAD_REQUEST_ERROR / invalid request sent` via direct curl (full refund, amount-specified, both fail) on a genuinely captured/refundable ₹448 card payment with matching keys → **it's an account-level refund-credits/permission restriction, not a code bug. Dilip's Razorpay setup: enable/top-up Refund Credits in the test dashboard (Settings → refunds), then re-run the refund.** Harness note: native browser clicks on this admin build's buttons didn't always register as React events — drove confirmed buttons via their React onClick in those cases; product behavior is unaffected. Test fixtures left in the DB (cleanup at Dilip's discretion): products "E2E Fee Test Tee A/B" (tags test-data,e2e-money), accounts claude.e2e.customer/admin@fitzo.test, order FTZ-2026-00051 + its payment, payout rows tagged E2E-TEST-UTR-00{1,2}.
- 2026-07-10: **Track A Task 2 — Razorpay `payment.captured` webhook** (branch `feat/money-hardening`, **migration 039**, pending apply). Kills the "phone dies right after paying → order stays unpaid" hole: settlement no longer depends on the browser success handler alone. Design: 009's settle body extracted verbatim into an ungranted `settle_keep_payment()` core (client path and webhook path share ONE implementation — can't drift); `confirm_keep_payment` recreated as a thin wrapper (**same args/behavior/error messages — no contract change**); new `razorpay_webhook_captured(raw_body, signature)` RPC verifies the WEBHOOK HMAC **in-DB** against a new Vault secret `razorpay_webhook_secret` (the customer app only has the anon key, so — like 009 — the RPC must be unforgeable without the secret; granted to `anon` deliberately, the signature IS the authorization). Double-settle race (client + webhook both firing): both paths lock the payments row FOR UPDATE and no-op on 'success'. Webhook RPC also checks captured-amount-vs-initiated (paise) and inlines 019's finalize steps (019's RPC is owner-gated on auth.uid(), which a webhook doesn't have). Route: `apps/customer/app/api/razorpay/webhook/route.ts` — raw-body HMAC pre-check with `RAZORPAY_WEBHOOK_SECRET` env var (belt-and-braces; generated + appended to Dilip's `.env.local`, also in `.env.example`), response codes drive Razorpay retries (401 forgery / 200 ignored / 500 retry). **Verified live in preview**: no-sig + bad-sig → 401, valid-sig non-captured → 200 ignored, valid-sig captured → RPC (500 = PGRST202, function missing pre-039 → Razorpay would retry, correct degrade). typecheck + customer build pass. **Dilip's steps (owns Razorpay + money flow as of 2026-07-11): apply 039, create the Vault secret + Razorpay dashboard webhook (exact steps in 039's header + the PR), then a live UPI test. Also Dilip's: regenerate the shared Razorpay test key secret (it's in chat history).**
- 2026-07-10: **Track A Task 5 — payout disbursement: investigated, deliberately NOT built** (branch `feat/money-hardening`, **migration 042** for the ledger hardening, pending apply). Investigation (sources in `docs/PAYOUTS-GOING-LIVE.md`): (1) **RazorpayX** — payouts need a RazorpayX account: separate signup from the PG, requires a **registered business entity** (Business PAN/GSTIN) + 2–3 day review + its own KYC; API keys come from the X dashboard, so even its (real) sandbox is unreachable until the account exists; RazorpayX Lite is closed to new merchants. (2) **Route** (split-settlement, the elegant store-payout architecture) — **RBI Sept-2025 PA guidelines gate it behind >₹40L domestic turnover**, no exemptions; off the table until scale. Verdict per the track rule ("don't fake it"): code written today is untestable against anything → wrote the go-live checklist + integration sketch (contacts/fund accounts from 029/034 data, payouts API with ledger-row idempotency, payout webhooks flipping processing→paid) instead. **Hardening shipped**: 042 adds `reference` (UTR/UPI txn id, typed in both record-payout modals) + `paid_to` (masked destination snapshot at record time — survives later edits to rider/store payout details) to both ledgers; store destination now fetched from `store_business_details` at record time (agent side already had it); modal copy stops implying automation ("Ledger entry only — make the transfer yourself"); all audit-logged with the new fields. Pre-042 degrade: inserts retry without the new columns. typecheck + admin build pass; **browser check (Dilip, after 042): record a payout with a UTR → toast, ledger row carries reference + paid_to (visible in Activity Log detail).**
- 2026-07-10: **Track A Task 4 — admin-initiated refunds, scoped MVP** (branch `feat/money-hardening`, **migration 041**, pending apply). Payment Records stops being read-only by exactly ONE action: **Refund** on successful Razorpay rows. Design: money moves via **Razorpay's refund API** (SDK added to apps/admin; keys server-only in admin `.env.local` — no NEXT_PUBLIC, admin never opens Checkout), through the established admin mutation pattern (`requireAdmin()` + service-role write + `logActivity`) — **no new RPC/RLS**; customers already SELECT their own payment rows so they see `refunded` status for free. Chose to **flip the existing payments row** success→refunded (the enum + the screen's Refunded tab existed since day one) over inserting a negative/mirror txn row — keeps Total-Captured/order-detail sums honest without sign conventions; `razorpay_refund_id`/`refunded_at`/`refund_reason` (041) carry the bookkeeping. Ordering hazards handled: (1) 041-columns **pre-flighted BEFORE the Razorpay call** — a pre-migration click can't move money the ledger can't record; (2) if the ledger write still fails post-refund, the error says loudly the refund WAS issued; (3) concurrent double-click → status-guarded update no-ops + Razorpay rejects a second full refund as backstop; (4) Razorpay-side "already fully refunded" (e.g. via their dashboard) **reconciles** our row instead of erroring. Scope cuts (deliberate): full refunds only, no partials; no automatic order/item state changes (admin adjusts via order-detail actions); no customer-facing self-serve. typecheck + admin build pass. **Browser test (Dilip, after applying 041): Admin > Payments → Refund on a success row → reason → confirm → row flips to Refunded (blue) + Activity Log entry; Dilip confirms the `rfnd_…` in the Razorpay test dashboard.**
- 2026-07-10: **Track A Task 3 — delivery fee collected on the first Keep charge** (branch `feat/money-hardening`, **migration 040**, pending apply). Owner chose option 1 (fold into the first Keep charge; no extra payment prompt; returns-everything = fee uncollected, accepted churn cost). Implementation: 040 adds `payments.delivery_fee_component` (attribution split — needed for admin display, Task-4 refunds, reporting); `createKeepPayment` charges item + `orders.delivery_fee` unless a successful payment already carried the fee, stamps the split, and puts the fee in the Razorpay order notes; `payments.amount` stays the FULL charge so 039's webhook amount check holds. UX: tracking page shows an amber "₹X delivery fee is added to your first Keep payment" note while uncollected + the Checkout modal description says "(incl. ₹X delivery fee)" — no surprise totals. **Pre-040 degrade verified against the live DB**: the fee check errors (42703) → bare item charge, old behavior, in both the action and the page. Known limit (documented in 040): the collected-check lives in the server action, so two simultaneous Keeps from two tabs could double-carry the fee — UI serializes, refunds (Task 4) cover the freak case. Admin Money-card caveat updated. typecheck + customer/admin builds pass. **Live test needs 040 applied + a real keep (Dilip owns Razorpay testing): sub-₹999 order → first Keep modal shows item+fee and the note disappears after; second Keep is bare item price.**
- 2026-07-10: **Track A (money hardening) Task 1 — admin Money card margin repointed to `rider_fee`** (branch `feat/money-hardening`). The order-detail Money card read `orders.delivery_fee` as the rider cost (pre-037 assumption "rider gets the full delivery fee, so margin = commission"). Now: rider pay = `orders.rider_fee` (₹0 pre-037, same degrade as the rest of the panel), a new "Delivery charge (customer)" line, and **margin = commission + delivery charge − rider pay** — with the footnote caveat that prepaid orders don't actually collect the delivery charge through Razorpay yet (Track A Task 3), so a non-zero charge can overstate margin until fee collection ships. typecheck + admin build pass.
- 2026-07-09: **Money — decouple rider pay from the customer delivery charge** (branch `fix/checkout-delivery-fee`, **migration 037**). Diagnosis first: the "riders earn ₹0" report was NOT a calc bug — the free-delivery threshold (`free_delivery_above=999`) correctly zeroes the CUSTOMER delivery fee on ₹999+ orders, but `orders.delivery_fee` was ALSO the rider's pay, so the rider earned nothing for the same trip (confirmed in the data: sub<999 → fee 49, sub≥999 → fee 0). Owner chose the "separate rider-pay config" option. Fix: `system_settings.rider_fee` (default 40, platform cost) + `orders.rider_fee` stamped at checkout **always** (no waiver) + backfill of existing orders; agent Earnings/History + admin agent-payouts `compute.ts` repointed from `delivery_fee` → `rider_fee` (renamed `CompletedDelivery.deliveryFee` → `riderFee` throughout for clarity); `delivery_fee` stays the customer charge. Admin System Settings → Commission & Payouts gains a "Rider Pay per Delivery" field. **Deliberately did NOT touch `available_deliveries()`** to show rider_fee on the offer card — that RPC was just changed in the unmerged 036, so re-touching it across branches risks migration-ordering regressions (037 would need 036's freshness filter + offer_expiry column to exist); deferred to a follow-up after #36 merges. typecheck (customer/agent/admin) passes; **037 pending apply + a browser check (new order stamps rider_fee, earnings show it) is Dilip's step.**
- 2026-07-09: **Reliability — offer expiry + stale-order cleanup** (branch `chore/agent-offer-expiry`, **migration 036**). From the loop test: confirmed-but-never-claimed orders (old test data) kept ringing riders as "waiting 25512 min" cards and sat in `confirmed` forever. 036 adds `system_settings.offer_expiry_minutes` (config, default 120 — same pattern as try_window/commission, not hardcoded), a freshness filter on `available_deliveries()` so stale jobs stop being offered immediately (self-heals with no cron), and `expire_stale_offers()` — a service-role sweep that terminally cancels an abandoned order + fails its unclaimed delivery + notifies the customer ("no rider found, not charged, try again"). Optional pg_cron block schedules both this and 027's `expire_try_windows`. Frontend: capped the agent offer card's waiting-time label. Chose to measure freshness from `orders.created_at` (no `confirmed_at` column; adding one is a future precision refinement) and to keep `expire_stale_offers` cron-only (REVOKE FROM PUBLIC) since it's an ops sweep, not a user action. typecheck + agent build pass; **036 pending apply + a browser check that ancient offers disappear is Dilip's step.**
- 2026-07-09: **Bug fixes from Dilip's testing — notifications + store onboarding** (branch `fix/notifications-and-store-onboarding`, no migration). (1) **Store onboarding "Check status" was dead** — the `submitted`/under-review screen's button called `router.refresh()`, which only refetches server components and never re-runs the client `loadOnboarding` effect (deps `[storeId, router]` never change), so an approved store kept showing "under review" until a full page reload (exactly Dilip's repro: button did nothing, refresh logged them in). Fixed: extracted the load into a `loadState` useCallback, "Check status" now re-fetches and redirects to `/` on approved, with a "Checking…" state + a "Still under review — checked just now" line when it's not yet approved. **Browser-verified**: flipped a seeded store submitted→approved mid-session; the button redirected into the dashboard with no manual refresh. (2) **Notification bells were "stuck"** — the ephemeral realtime alert feeds in admin (`OrderAlerts`), store (`OrderAlertsProvider`/`AlertBell`) and agent (`JobAlertsProvider`) accumulated up to 50 items with **no Clear action**, and clicking a bell-history item navigated but left it in the list forever. Added a **"Clear all"** button (shown when the list is non-empty) to all three, and made clicking an item **remove it from the history**, not just the pop-up stack. (3) **Found while fixing**: the admin new-order pop-up header rendered `text-ink` on `bg-ink` (invisible dark-on-dark — same class-collision family as the agent try-window card) — fixed to white, and dropped the emoji for consistency with store/agent. Customer notifications page was already correct (DB-backed, mark-read + mark-all-read) so left as the reference. typecheck + builds (store/admin/agent) pass.
- 2026-07-07: **Agent panel rework → PR #33 → merged to main** (https://github.com/Dilip290703/fitzo/pull/33, owner-authorized). One grouped PR: phases 0–4 + live-loop fixes; migrations 033–035 all applied. Full three-panel order loop verified live before merge (see the live-fire entry below).
- 2026-07-11: **Profile fixed to a true Snitch account shell — killed the triple-duplication** (branch `feat/customer-ui-refresh`, J). The prior redesign showed Orders/Addresses **three times at once** (stat tile + menu row + a content panel of the same name sitting beside the menu). Restructured to Snitch's actual pattern: the **left menu is navigation only**, and the right column renders **exactly ONE view** at a time (`activeView` state). Menu rows are now typed — `view` rows (Overview / Addresses / Coupons) swap the right panel in place with an active/highlighted state; `href` rows (Orders / Wishlist / Notifications / Help) navigate to their own pages. Dropped the 3 stat tiles entirely (they duplicated menu destinations). New **Overview** view = a dark FITZO-MUSE welcome banner + a single "order in progress" card (status only — full history lives at `/orders`, not duplicated here). Deep links preserved: `/profile#addresses` (used by checkout) opens the Addresses view via a `hashchange` listener, and view switches `replaceState` the hash without stacking history. All address CRUD / coupons markup unchanged, just moved under their view conditionals. Verified logged-in: menu has one entry per destination, switching Overview↔Addresses↔Coupons swaps the single panel + highlights the active row + syncs the hash, and `/profile#addresses` deep-links correctly. typecheck + 3 vitest pass.
- 2026-07-10: **Customer UI refresh round 3 — profile rebuild + catalog card sizing** (branch `feat/customer-ui-refresh`, J). (1) **Profile page fully redesigned Snitch-style** (`ProfilePanel.tsx`): dropped the maximalist "everything dashboard" (giant black hero, 11-card action grid, AI Style-Preferences panel, Style-Match stat) for a light layout — compact header (avatar + "Hi, {first name}" + Edit/Logout), 3 clickable stat tiles (Active orders / Wishlist / Addresses, wishlist count now real via `useWishlist`), a clean left **account-menu list** (My Orders, Wishlist, Saved Addresses, Coupons & Rewards, Notifications, Help & Support + Change password / Delete account) with SVG icons + chevrons, and a right column of Recent orders (empty-state aware) + Address book + Coupons. **All AI removed** (owner request) — the `/ai-style-setup` panel + action card + "Style Match" stat are gone; every data/CRUD handler (profile edit, address add/edit/delete/default, logout, toast) preserved verbatim. (2) **Catalog cards were "massive"** because `/products` had **no max-width** — the grid stretched edge-to-edge (4 cols ≈ 390×520px on a wide monitor). Capped the results section at `max-w-[1360px]`; keeps the 4-col grid (owner: "4 is good") but cards are now ~244×325px at 1440px. (3) **Entrance-animation robustness lesson (important):** the in-app **preview renderer pauses time-based animation clocks** (`getAnimations()` → `playState:"running"` but `currentTime:0` forever) — this is what made *every* screenshot this session look faded (framer + CSS alike) and it stranded framer mount-fades at `opacity:0`. Fix: the profile entrance is now a **transform-only CSS `.rise-in`** (`globals.css`) — opacity is never animated, so content is visible even if the clock never advances (paused/throttled webviews); worst case a block rests 12px lower. Do NOT gate visibility of critical content behind an opacity-fade entrance. Verified logged-in via a throwaway signup (`uitest…@fitzo.dev`) — profile renders, AI absent, all blocks opacity 1. typecheck + 3 vitest pass. **Test data to clean:** the `uitest*@fitzo.dev` account.
- 2026-07-10: **Customer UI refresh round 2 — Jay's live feedback pass** (branch `feat/customer-ui-refresh`, J). (1) Hero H1 → **"The fitting room comes to you."**; "Try before you buy" demoted to a letterspaced tagline under it. (2) **Category strip** is now a curated owner-approved 6-chip set (Women / Men / Kids / Footwear / Hot Picks / Sale) spread edge-to-edge; photos are **Jay's own assets** in `public/categories/` (rule: never auto-pick stock — list needed files and wait). His hot-picks source was a third-party ad (LocalBrands.com logo + store badges) → **cropped via sips to just the subject, zero foreign branding**; his sale image (Pacific-mall ad with baked-in offer dates) was unusable at any crop → Sale keeps the ink `%` circle until a clean asset lands. Women/kids/footwear chips fall back to the full catalogue until those `categories` rows exist (the /products page's own honest fallback). (3) **New `?sort=` URL param** on /products (Hot Picks deep-links to `?sort=popular`); normalizeSort extracted to `lib/sort.ts`. (4) Homepage hot-picks grid 6→**4 across, 8 products** (cards were "gross"-small at 6-up). (5) Navbar: icon hover reduced to a **single animation** (darker creamed-oat circle — underline/lift/scale removed); **profile icon only renders with a live session** — logged out shows just the Login/Signup pill. (6) Fixed `LoginRequiredModal` staying painted over /login: its CTA navigated without `onClose()`, and the modal is provider-mounted (root layout) so it survives client route changes — close-then-push. (7) **First customer-app tests**: vitest wired (`pnpm --filter @fitzo/customer test`, mirrors the store panel's zero-config pattern), 3 tests on `normalizeSort`; also fixed the fill-image-in-static-parent warnings in both ProductCards (missing `relative` on the Link wrapper). All browser-verified in preview (modal flow driven end-to-end; sort preselect + 16-product grid confirmed).
- 2026-07-09: **Customer UI refresh — luxury beige rebrand + landing & catalogue redesign** (branch `feat/customer-ui-refresh`, J). (1) **New palette** (Feather White #faf9f6 / Porcelain Mist #f4f1ea / Creamed Oat #e6dac8 / Soft Sandstone #cbb9a4 / Desert Rock #a48d78, ink #221b13 text tones) applied panel-wide via CSS vars + a hex sweep of every old cream/gold/navy value; fonts moved to next/font **Playfair Display** (display) + **Jost** (body). (2) **Landing rebuilt to Jay's references**: light editorial hero with a vertical auto-scroll photo carousel (Jay's 3 Pinterest images in `public/hero/`, clickable 01/02/03 rail, wipe-hover CTA), trust strip folded into the hero and replaced by a **category chips band** (chips render only genders that exist in `categories` — currently Men + Sale), 6-across hot-picks cards (star row coded but **hidden until real reviews exist**), **dual dummy promo banners** shaped like the future admin `banners` table row, testimonials as a CSS marquee (pause + glow on hover, half-star ratings 5.0/4.5/4.5/4.0). Deleted dead components: TrustStrip, BrandsForYou (duplicate brand section), SummerBanner/ExperiencePreview + their mock data. (3) **/products rebuilt Snitch-style**: centred title + real gender pill tabs (fake Ethnic/Collections tabs and the fake "80% off" banner removed), far-left sticky filter rail, **inner-scroll grid removed** (it hid pagination inside a nested scrollbar) — normal page flow + windowed pagination + scroll-to-top on page change. (4) **Navbar**: logo was absolutely centred and overlapped the pincode pill on narrower viewports → 3-column grid; nav active-underline was pathname-only so PRODUCTS/CATEGORIES/SALE all lit together → query-aware matching (Navbar wraps itself in Suspense for useSearchParams). All browser-verified (pagination click-through, tab filtering, all three underline states, 1008px + 1280px widths). No automated tests — customer app has no test runner (see Known issues). ⚠️ **2026-07-10 audit correction:** the PR #33 link + "migrations 033–035 applied" tail that used to end this entry belonged to the agent-rework entry (PR #33 = agent rework). The UI refresh lives on branch `feat/customer-ui-refresh` and is **UNMERGED** — 5 commits ahead / 7 behind origin/main as of 2026-07-10, plus an uncommitted animation pass in the working tree (framer-motion via new `components/motion.tsx` + `app/template.tsx`, 3 new hero slides).
- 2026-07-07: **FULL ORDER LOOP LIVE-FIRE (preview, all three panels)** — customer signup → Sample Oversized Tee ordered (FTZ-2026-00049, COD verified GONE from checkout) → store "Ready & confirm" → offer card v2 rang on the rider (store name/area/age, PII-redacted drop) → accept → readiness-gated pickup → arrive → **doorstep OTP verified live** (wrong code rejected with the in-DB message, right code 5296 handed over) → 7-min waiting-room timer live → customer returned the item → collect checklist ticked → complete → rider credited in history/earnings. **Two real bugs found & fixed during the run:** (1) `Card className="bg-ink"` ties with the Card's own `bg-white` (equal Tailwind specificity → stylesheet order wins) leaving WHITE-ON-WHITE timer text — try-window + guide welcome cards are now plain ink divs; (2) **migration 035 (applied)**: `finalize_order_if_decided` (019) completes the order on the customer's last decision but never the delivery — 033's guard then bricked the delivery at 'arrived' (rider uncredited, cap blocked); `rider_complete_delivery` now also closes out from order-'completed', and the detail page keys its finished state off the DELIVERY (new awaiting-collect state shows the checklist post-finalize). Also cleared 6 stuck legacy pre-033 deliveries via the new rider fail path (auto-filed complaints, marked "please close") + 1 released to pool. **Test data to clean when convenient:** customer `fitzoloop.customer@gmail.com`, placeholder rider payout details (PAN ABCDE1234F / UPI fitzorider@okhdfc), abandoned order FTZ-2026-00048 (pending, Air Jordan store never confirmed), 7 rider-filed complaints. **Improvement list** (from the run, not blocking the PR): checkout collects only a pincode — no street address form, so riders get "Address on file/demo order" drop cards; `delivery_fee` setting is 0 so every job pays ₹0; offer-age label reads "waiting 25512 min" on stale orders (cap at days); stale confirmed orders (weeks old) still offer to riders (needs expiry/sweep); storefront /products & /search show static placeholder cards rather than live products; footer still says "We accept … COD".
- 2026-07-07: **Agent panel rework Phase 4 — remaining screens + polish sweep** (branch `feature/agent-panel-rework`, no new migration). (1) **Support went live**: riders raise tickets on the existing `complaints` table (same zero-migration pattern as the store panel — `complaints_insert_own`/`_select_own` already cover a rider's users row; subject prefixed `[Rider: name]` for admin triage) + a "My tickets" list showing status chips and `admin_response` replies from Admin > Complaints. FAQ + Guide rewritten for the Phase 2 flow (offer card, store-ready gate, arrival + 4-digit code, collect checklist, Problem? sheet). (2) **Honest error states (audit M4)**: the five list fetchers (`fetchMyDeliveries`, `fetchCompletedDeliveries`, `fetchNotifications`, `fetchAgentPayouts`, `fetchMyTickets`) now return `{rows, error}` instead of swallowing failures as `[]`, and Dashboard/Deliveries/Earnings/History/Notifications render a shared `ErrorCard` with Retry instead of a fake "all caught up" empty state on a dead connection. Earnings deliberately only hard-fails on the jobs query (a pre-020 missing payout ledger shouldn't brick the screen). (3) **One mute switch (audit M7)**: both alert providers now share `fitzo-agent-alerts-muted` + a same-tab `fitzo-mute-change` CustomEvent so muting the offer card also mutes job alerts instantly (same-tab localStorage writes don't fire storage events). (4) **Audit-logging check**: no gaps — rider writes are SECURITY DEFINER RPCs (not admin actions); everything admin-visible this rework added lands in existing audited surfaces (complaints; `recordAgentPayout` already calls `logActivity`). typecheck + build (13 routes) pass; login verified zero-overflow at 375×812. **The logged-in phone pass across all screens + the full delivery live-fire (needs 033+034 applied) is Dilip's step.**
- 2026-07-07: **Agent panel rework Phase 3 — money + profile** (branch `feature/agent-panel-rework`; migration **034**, pending apply). (1) **Rider bank/UPI + PAN capture** (audit B2 — Admin > Agent Payouts finally has a destination): new private `rider_payout_details` table mirroring `store_business_details` (RLS: own rider or admin, NO rider write policy — writes via guarded `save_rider_payout_details` RPC that validates PAN/IFSC/UPI/account formats **in-DB**, since unlike stores there's no submit/approve review step to catch bad data). Deliberately NO approval loop — `riders.is_verified` stays the only gate; payout details just need to exist. Agent Settings gains the payout form (method toggle, On file/Missing chip), Profile a status row. (2) **Earnings v2**: wired to `agent_payouts` (rider's own-rows RLS from 020 was never read by the panel until now) — "Paid out" + "To be paid" (earned − settled) cards, per-job **Paid/Due chips** (keyed rider+order, same as admin's ledger), payout-history section, and a warn banner → Settings when payout details are missing. `CompletedDelivery` now carries `order_id` for the chip join. (3) **Admin > Agent Payouts** shows a "Pay to" column (masked account ····1234 · IFSC, or UPI id) + the destination inside the record-payout confirm — with an explicit red warning when recording against a rider who has no details. (4) **COD hidden at checkout** (owner-approved SIMPLIFY from the Phase 0 audit): the keep flow is Razorpay-only and riders have no cash step, so a COD order could never settle; `payment_method` enum + 'cod' mapping kept for old orders; re-add only with a rider cash ledger. All panels degrade gracefully pre-034. typecheck (repo) + agent/admin builds pass; **applying 034 + a live save/payout round-trip is Dilip's browser step.**
- 2026-07-07: **Agent panel rework Phase 2 — the delivery flow** (branch `feature/agent-panel-rework`; migration **033**, pending apply). (1) **Rider finally knows where to pick up** (audit #1): `store_confirm_order` stamps `pickup_address` (first store + `store_count` for multi-store orders) + backfill; delivery detail leads with a Pickup card (store, address, call store, Maps) until picked up. (2) **Offer feed v3**: 1-concurrent-job cap, customer PII redacted pre-claim (`drop_area` = city/pincode/landmark only — full address+phone appear only after claiming via RLS), store name/area + order age on the card, `final_amount` dropped. ⚠️ **Contract changes for Jay** (all in 033's header): `available_deliveries()` return shape changed; `rider_mark_delivered(uuid)` dropped → `(uuid, text)` OTP version requiring the new `rider_mark_arrived` step first; `rider_mark_picked_up` now errors until the store marks every item ready. (3) **Doorstep OTP handover**: customer's tracking page shows a 4-digit code while out-for-delivery (via owner-gated `get_delivery_handover` RPC — customers can't SELECT deliveries under RLS), rider enters it at the door; legacy no-OTP deliveries skip the check. (4) **Money-path integrity** (audit D2): `rider_complete_delivery` now requires arrived + try_window_active + (all decided OR deadline passed) — the console-callable instant-complete hole is closed. (5) **Bad-day exits** (audit C3): header "Problem?" → bottom sheet with call customer/support, **Report an issue** (rides the existing `complaints` table like the store panel — no new table) and **Can't deliver — end this job** (`rider_fail_delivery`: reason presets, delivery failed + order cancelled + auto-filed high-priority complaint; rider returns items to store). (6) **Waiting room**: try-window card gains call-customer; **collect-returns checklist** — rider must tick each returned (+expired-pending) item before "Collect returns & complete" enables; screen **wake-lock** while a delivery is active; offer/alert chimes now also **vibrate** (audio needs tap-unlock, dies in pockets). **No distance on offers — no geo data exists anywhere; shows order age instead.** Client degrades gracefully pre-033 (PGRST202 fallbacks; offer card shows "Store on accept"). typecheck (agent+customer) + builds pass; preview-verified at 375px logged in (dashboard, detail, problem sheet + report form). **Full live-fire (order → confirm → offer → OTP → checklist → complete) needs 033 applied + a real order — Dilip's browser step.**
- 2026-07-06: **Agent panel rework Phase 1 — design system + mobile-first shell + PWA** (branch `feature/agent-panel-rework`, cut from main post-PR #32; Phase 0 audit in `docs/AGENT_PANEL_AUDIT.md`, owner approved the recommended calls). (1) **Tokens ported** — the store/admin paper/ink/gold `@theme` block lands in `apps/agent/app/globals.css` at rider scale (≥44px targets, h-14 primary actions, 14px body); the hand-rolled dark-navy theme + every raw hex class swept from all screens (light theme also wins in sunlight). (2) **`AgentShell` rebuilt mobile-first** — sticky header with a big 44px Online/Offline pill + floating alerts bell, 5-slot bottom tab bar (Home/Jobs/Earnings/Alerts/More) with a **new SVG icon set** (`components/icons.tsx`, emoji killed app-wide), "More" is now a bottom sheet (z-[80] — deliberately above the offer overlay's z-[70] so open menus aren't buried under offer cards); desktop sidebar restyled. (3) **PWA installable** — `public/manifest.webmanifest` + generated 192/512 icons + themed viewport/appleWebApp metadata in `layout.tsx` (`apps/agent/public/` didn't exist before). (4) **Owner-approved value tweaks folded in:** fake 5.00 rating hidden (nothing writes `riders.rating`; dashboard stat → all-time deliveries, profile stat → vehicle), stale "admin assigns you jobs" copy fixed to offer-flow copy on Deliveries/Dashboard/Guide, Earnings "Recent payouts" relabeled "Completed jobs" (they aren't payouts), skeleton loading states replace bare "Loading…" on list screens. Also fixed a real React duplicate-key bug in the earnings 7-day chart (keyed by narrow weekday "T"/"S"; now by date). **No RPC/data-contract changes** — providers' polling/chime/claim logic untouched. typecheck + build (13 routes) pass; **logged-in 375×812 pass done in preview** (saved test-rider creds worked): dashboard/jobs/earnings/history/alerts/profile/settings/support/guide + delivery detail + login all zero-overflow, offer card + More sheet verified live. COD hide-at-checkout deferred to Phase 3 (money), doorstep OTP to Phase 2 (flow).
- 2026-07-06: **Admin panel rework → PR #32** (https://github.com/Dilip290703/fitzo/pull/32, base `main`). One grouped PR carrying phases 0–4 + the dashboard SSR-crash fix (server page must not call functions exported from a 'use client' module — helpers moved to `active-deliveries-lib.ts`). Migration **032 applied**; full logged-in browser pass done by Dilip 2026-07-06. Branch was cut from `feature/store-panel-upgrade`, whose PR #31 merged 2026-07-03, so the diff vs main is admin-rework-only.
- 2026-07-04: **Admin panel rework Phase 4 — remaining screens + polish sweep** (branch `feature/admin-panel-rework`). (1) **Activity-log coverage audited: complete** — all 13 mutating clients/actions call `logActivity`, zero gaps. (2) **Confirm-dialog gaps closed:** rider Verify/Revoke (gates the agent panel) + delivery Release now confirm; **new delivery "Fail" action** (confirm + log) on unassigned AND active rows — `delivery_status='failed'` finally has an admin path (audit A2a). (3) **Store-detail Recent-Orders bug fixed** (audit A6.1): was `orders.eq('user_id', storeId)` (customer column ↔ store id, always empty); now `order_items→products!inner(store_id)`, deduped to newest 10. (4) **Route-level `app/admin/loading.tsx` (skeleton) + `error.tsx` (retry card)** standardize pending/error states across all screens; empty-state sweep verified (every list has one, no bare "Loading…"). Migration **032 applied by Dilip**. typecheck + build (28 routes) pass. Rework phases 0–4 all committed on the branch — remaining known gaps deferred by scope: 2FA/email allowlist on login, refund/payment-retry path (Razorpay flows, partner), server-side pagination (pre-scale), CMS parked.
- 2026-07-04: **Admin panel rework Phase 3 — orders + money** (branch `feature/admin-panel-rework`, 3 commits). (1) **Migration 032** (pending apply, idempotent): dedupe + `UNIQUE(store_id, order_id)` on `payouts` — the double-payout guard `agent_payouts` already had; both record-payout actions turn a 23505 into a friendly "already paid out — refresh". (2) **Orders list:** try-window column fixed to minutes and only shown while active (old hours math said "Expired" on every live 7-min window); payment-status filter; tab changes sync to `?status=` (shareable); **saved filter views** (localStorage chips); **bulk actions** via new DataTable checkbox-selection — bulk-confirm pending (one `.in()` update, audit-logged) + CSV export. (3) **Order detail:** Stores card (items grouped via `products.store_id` — the missing order→store hop); **Money card** = per-order reconciliation (captured / kept gross / commission @ settings rate / store net paid-or-owed vs ledger / rider fee / margin, caveats labeled); per-item payment records listed; **merged timeline** (order+items+delivery+try session+payments+activity log); **OrderActions** now confirm-modal + reason (required on Cancel, logged), Cancel also fails live deliveries + expires active try sessions, and the try window finally reads `system_settings.try_window_minutes` (hardcoded 7 gone). typecheck + build (28 routes) pass; **logged-in walkthrough is Dilip's browser step**; migration 032 must be pasted into Supabase.
- 2026-07-04: **Admin panel rework Phase 2 — action-first dashboard** (branch `feature/admin-panel-rework`). Dashboard now leads with a **"Needs attention" queue list** (8 checks: expired-stuck try windows, kept-but-unpaid orders, failed payments 7d, pending >30min, unclaimed deliveries, open complaints, store applications, store+agent payouts due — payout amounts reuse `payouts/compute.ts` + `agent-payouts/compute.ts` so the dashboard can never disagree with the payout screens). Rows deep-link into the fix; Orders/Payments/Complaints gained `?status=` tab preselection for this. Second section: today's numbers (clickable StatsCards incl. keep-rate-today) + **ActiveDeliveries** (`app/admin/ActiveDeliveries.tsx`) — the Live-Map replacement: realtime list (deliveries subscription, admin `is_admin()` RLS routes fine) with tap-to-call rider/customer + per-row Google-Maps link. Charts moved last; the recent-orders table was dropped (duplicate of Orders screen); low stock demoted to a footer strip. typecheck + build (28 routes) pass; **logged-in dashboard walkthrough is Dilip's browser step** (no admin creds in repo).
- 2026-07-04: **Admin panel rework Phase 1 — security lockdown + cuts + shell** (branch `feature/admin-panel-rework`, 3 commits). (1) **Security:** middleware/layout/login accept `role='admin'` ONLY (store_manager was admitted — public signup role!); new `lib/require-admin.ts` `requireAdmin()` is now the first line of every service-role server action (8 files); stores-list + inventory/new pages read via SSR client instead of service-role. (2) **Cuts approved in Phase 0:** `/admin/live-map` deleted (dashboard Active-Deliveries list lands in Phase 2); Try-analytics folded into `/admin/analytics`; Brands+Categories merged into `/admin/taxonomy` (tabs); CMS parked (route alive, out of nav). (3) **Shell:** admin now uses the Store panel's semantic tokens (`@theme`, light paper/ink/gold — one product family) at denser rhythm; new `AdminShell` = grouped sidebar (Operations/People/Money/Catalog/Growth/System, SVG icons, collapsible) + header with **global ⌘K search** (order#/phone/email/name/store → jump). Full dark→light class sweep across all screens (zero raw gray/indigo classes left). ⚠️ Sweep was mechanical (sed) — typecheck+build+login-page verified, but the **logged-in visual pass over all screens is Dilip's browser step** (no admin creds in the repo). Dev note: macOS/BSD sed has no `\|` alternation — a guarded pass silently no-opped and needed a repair pass; also zsh doesn't word-split unquoted vars (use bash for such scripts).
- 2026-07-03: **Admin panel rework Phase 0 — audit only, nothing changed** (branch `feature/admin-panel-rework`). Full diagnostic in **`docs/ADMIN_PANEL_AUDIT.md`**. Headlines: 🔴 admin middleware/layout/login admit `role='store_manager'` (now a PUBLIC self-serve signup role → anyone can enter the panel) and **no service-role server action checks the caller's role** — `changeUserRole` allows escalation to full admin via a second account. Also: `payouts` lacks the `UNIQUE(store_id,order_id)` that `agent_payouts` has (double-payout race); orders-list try-deadline renders hours for a 7-min window (always "Expired"); store-detail recent orders queries `orders.user_id = store.id` (always wrong); CMS `content_blocks` has zero consumers; no refund path exists anywhere. Customer-bundle security check: **clean** — no admin code/keys in `apps/customer`; residual risk is RLS regression, not bundle leakage. Cut list recommended (map CUT needs zero schema cleanup — no GPS data exists), awaiting Dilip's approval before Phase 1.
- 2026-07-03: **Store panel upgrade → PR #31** (https://github.com/Dilip290703/fitzo/pull/31, base `main`). One grouped PR carrying all four phases + the mobile fix commit + the stacked `feat/store-support-hardening` base commit (a0bde91, never separately merged). Post-Phase-4 addendum: the logged-in **375px mobile pass is DONE** (preview tab held a session) — found & fixed real squeezes the static audit missed: dashboard queue cards now stack on mobile, atomic table cells (order nos/dates/amounts/badges) are nowrap so tables scroll sideways, order-detail item rows keep a 180px text minimum, Earnings grid children got `min-w-0` (killed a 3px page overflow). All 12 authed screens + login verified zero-overflow at 375×812. Only migration **031** pending (optional — fallback works without it).
- 2026-07-03: **Store panel upgrade Phase 4 — remaining screens + polish sweep** (branch `feature/store-panel-upgrade`; closes the audit's Top-10). (1) **`PageHeader` primitive** (eyebrow/title/sub/action) replaced the last big copy-paste — the header block in all 10 panel views. (2) **Settings** got inline per-field validation (email + `PHONE_RE`/`PINCODE_RE` promoted to `lib/onboarding.ts` exports, shared with the wizard) with digit-only inputs. (3) **Returns** filter chips show counts like Orders. (4) **Support** tickets loading state = skeleton (last bare "Loading…" in the panel). (5) **Polish/mobile sweep**: login verified clean at 375×812; static audit confirms every table is wrapped in `overflow-x-auto`, action rows use `flex-wrap`, and the only fixed widths are the 300px alert popups (fit 375). typecheck + build (17 routes) + 23 tests pass. ⚠️ The logged-in preview session from Phase 3 was lost (fresh preview profile), so the **logged-in mobile spot-check (dashboard queue, orders table, product form at 375px) is Dilip's browser step** — device toolbar on :3003. Also recurring dev-server gotcha bit again: a stray `next dev` process survived a preview stop and was running during the build → chunk 404s; fixed by `lsof -ti :3003 | xargs kill` + clean `.next` + restart (see the Phase 3 note — always check the port, not just the preview manager).
- 2026-07-03: **Store panel upgrade Phase 3 — order & catalog flows** (branch `feature/store-panel-upgrade`). (1) **Migration 031** `mark_order_items_prepared(order_id, ready)` — bulk mark-ready in ONE round-trip (SECURITY DEFINER; only flips the caller's own store's items via `is_store_manager_of`, so multi-store orders are safe by construction; only touches rows that change so retries keep first-ready timestamps). ⚠️ **Client falls back automatically** to the per-item RPC when 031 isn't applied (PostgREST PGRST202) — the panel works either way; apply 031 to make "Ready & confirm" single-round-trip. (2) **Orders list**: filter chips now show counts, order-number search, per-row **"Ready & confirm"** on pending rows, and the list **live-reloads** off the alerts provider's `pendingCount` + window focus (deliberately NOT a raw table subscription — Realtime can't route join-RLS events to store sessions, see the 2026-06-27 realtime note). (3) **ProductForm safety**: inline per-field validation (`validateFields` in lib, `validate` reimplemented on top — messages unchanged, tests intact), **new products default to DRAFT** (`isActive: false` + explicit "Live on storefront" toggle), dirty-state guard (Cancel → discard dialog + `beforeunload`), drag-to-reorder photos (HTML5 DnD, ◀▶ buttons kept for touch), and **no-duplicate saves**: a failed colours/variants insert soft-deletes the orphan product row before rethrowing, and an image-upload failure after a successful create routes to the edit screen instead of letting "Create" run twice. 23 tests pass. **Verified in a real logged-in browser session** (preview tab held a store session): dashboard one-tap confirm via the fallback path (FTZ-2026-00016 pending→confirmed, badges 8→7, persisted), chip counts + search, inline errors, draft hint, discard dialog. ⚠️ **Dev-ops lesson: never run `next build` while the dev server is running** — they share `.next`; the build corrupts the dev server's chunks (the ChunkLoadError / infinite "Loading…" Dilip hit). Fix = restart dev with a clean `.next`.
- 2026-07-03: **Store panel upgrade Phase 2 — dashboard as an operations home** (branch `feature/store-panel-upgrade`). The dashboard now leads with a **"Needs your action" queue**: every `pending` order (oldest first, via new `loadPendingStoreOrders` — store-scoped, includes line items) rendered as a card with order no., "placed Xm ago", item-name preview, subtotal, ready-count, and an inline **"Ready & confirm"** button that marks every unprepared item ready and confirms in one tap (with "Preparing 2/5… / Confirming…" progress). ⚠️ Deliberately still the **sequential per-item RPC** (migration 007) so nothing depends on an unapplied migration — Phase 3's bulk `mark_order_items_prepared` RPC will collapse it to one round-trip. Queue is **live**: the dashboard reloads whenever the alerts provider's `pendingCount` changes (new order chime / confirm elsewhere), no extra poll. The passive recent-orders table was replaced by an **Activity feed** (`loadActivityFeed`) merging three store-RLS-visible streams — orders placed, keep/return decisions (`decision_at`), payout ledger entries — with relative timestamps (`timeAgo` in `lib/format.ts`) and each row deep-linking to its order/earnings. Empty queue = green "All caught up". typecheck + build + 20 tests pass; route compiles & serves clean on a side dev server (:3013); **logged-in queue/one-tap-confirm walkthrough is Dilip's browser step** (his :3003 dev server hot-reloads this branch).
- 2026-07-03: **Store panel upgrade Phase 1 — audit + design system + persistent shell** (branch `feature/store-panel-upgrade`, stacked on `feat/store-support-hardening`). Full audit in **`docs/STORE_PANEL_AUDIT.md`** (Phase 0). Phase 1 shipped: (1) **Design tokens** — semantic color/shadow tokens via Tailwind v4 `@theme` in `apps/store/app/globals.css`; every raw hex class in the app swept to `text-ink`/`bg-paper`/`border-line`/… (value-preserving, zero visual change). (2) **Persistent shell** — authed routes moved into an `app/(panel)/` route group whose client layout runs `useStoreGuard` ONCE and provides the store context (`useStorePanel()`); pages are now tiny server components with per-route `<title>`s, and navigation no longer blanks the app or remounts the alert stream. Guard now distinguishes network failure (error + Retry screen) from not-a-manager (redirect) — a flaky connection no longer bounces to /login. (3) **Shell v2** — grouped sidebar (Operations/Money/Store) with SVG icons (no emoji), store name + live-dot at top, collapsible rail (localStorage), header bar with section title + integrated AlertBell + a "N to confirm" chip; `OrderAlertsProvider` became a context that also tracks a **pending-orders badge count** (refreshed on mount/alert/focus/confirm). (4) **Shared primitives** in `components/ui/` — StatCard (linkable), StatusBadge (5 tones), Banner, Field/inputClass, Skeletons, ConfirmDialog (Escape/backdrop/focus), Toast — adopted across all 14 screens; `lib/format.ts` replaces 7 copies of formatCurrency. (5) **Correctness fixes folded in:** store-scoped the Orders list + Dashboard queries via `order_items→products.store_id` (the base `orders_select` RLS also exposes the manager's PERSONAL customer orders — earnings/analytics already guarded this, orders/dashboard didn't); Dashboard "Pending payout" (always ₹0 — summed `payouts.status='pending'` rows admin never creates) replaced by **"Awaiting payout" from the same `loadPayoutSummary()` the Earnings page uses**, so the two screens can never disagree. Deferred by design: generic DataTable (Phase 3, when order/catalog tables are redesigned), inline per-field validation + dirty-state (Phase 3), dashboard action-queue (Phase 2). typecheck + build + 20 tests pass; tokens/guard-redirect verified in preview; **logged-in shell walkthrough is Dilip's browser step**.
- 2026-07-02: **Store rework stretch batch — support tickets + hardening + first tests** (branch `feat/store-support-hardening`). (1) **Store-side support tickets** ride the existing `complaints` table with **no new migration** — a store manager is a `users` row, so `complaints_insert_own`/`_select_own` (user_id = auth.uid()) already cover filing + reading; admin triages via the existing Admin > Complaints (#13). Subjects are prefixed `[Store: <name>]` for triage (complaints has no store_id column — a real column is a later migration if store tickets need admin-side filtering). Optional order attach resolves the typed order number under the store's own RLS and rejects unknown numbers rather than silently detaching. (2) **Hardening:** onboarding wizard gets a load-failure Retry state (it's the one screen a pending store is forced onto — must never hang on "Loading…"); new screens (wizard/signup/support) verified no-overflow at 375px. (3) **First automated tests in the repo** — vitest in `apps/store` (`pnpm --filter @fitzo/store test`), 20 smoke tests over the pure logic: onboarding KYC regexes (PAN/GST/IFSC/pincode, mirroring migration 029's server checks), product-form `validate`/`makeSlug`, image-file validation. Colocated `lib/*.test.ts` per code style.
- 2026-07-02: **Store rework — product images + commission-correct earnings** (branch `feat/store-images-earnings`). (1) **Product image upload finally wired** — the schema had `product_images` + a `product-images` bucket comment since day one but no write path: migration **030** adds the manager-write policy on `product_images`, creates the bucket, and adds storage policies scoped by the object path's first folder (`{store_id}/{product_id}/…` via `is_store_manager_of`). ProductForm gains an Images card (max 8 × 5MB, ★ cover, reorder, remove) that queues files locally and uploads on save — works in create mode too (uploads right after the product row exists). Customer storefront already reads `product_images`/`is_primary`, so store uploads appear on fitzo.in with zero customer-side changes. (2) **Store Earnings now shows NET** — reads `system_settings.commission_rate` and mirrors Admin > Store Payouts math exactly (per-order net = round2(gross × (1−rate/100))) so both panels show identical rupees; new gross − commission = net breakdown strip + Awaiting-payout card. Also fixed a real bug: gross totals were computed from the display query capped at `limit(50)` (under-reported past 50 kept items) — totals now come from an uncapped query. Migration 030 applied + both browser-verified by D.
- 2026-07-02: **Store rework — self-serve onboarding + admin approval** (branch `feat/store-self-serve-onboarding`). Chosen over admin-invite: public signup → wizard → `submitted` → admin approve (sets `onboarding_status='approved'` + `is_active` + `is_verified`) or reject (+reason, store edits & resubmits). KYC kept **lightweight (fields only)** — GST/PAN/legal-entity + bank/UPI as validated text, no document upload yet. Key data-model call: KYC/bank details go in a separate **private `store_business_details`** table because `stores` is world-readable for the storefront; RLS = `is_store_manager_of(store_id) OR is_admin()`, and there is deliberately NO store INSERT/UPDATE policy — writes go through guarded SECURITY DEFINER RPCs that lock once submitted. Migration **029** also adds `stores_manager_select` (a manager must see their own DRAFT store — the base policy `is_active OR is_admin` would have locked pending stores out of their own login) and back-fills pre-existing stores to `approved`. Admin approve/reject needs **no new migration** (`stores_admin_all` covers it) and is audit-logged via `logActivity`. Migration 029 applied + full loop browser-verified by D.
- 2026-06-28: **Core flow rework — rider self-serve, admin out of the critical path** (branch `feat/rider-self-serve-reliable-alerts`). The main loop is now **customer → store → agent**, independent of admin: customer orders (pending) → store marks items ready + Confirms (→ confirmed, delivery created rider_id NULL) → the delivery is offered to ALL online verified riders, who Accept/Decline; **first to claim wins** (`rider_claim_delivery`, atomic `WHERE rider_id IS NULL`). Admin assignment is now an **override, not a required step**. Migration **024** adds `available_deliveries()` (offer feed, verified-rider gated) + `rider_claim_delivery()` — both SECURITY DEFINER so no delivery-RLS widening. Agent `IncomingJobsProvider` **polls** the feed every 7s while Online with a **repeating chime** (driver-app UX) — polling chosen over Realtime because the rider offer is a broadcast to many users (join-RLS can't route it) and reliability > latency. Same reason the **store pop-up** switched to an 8s notifications poll (Realtime wasn't delivering to store sessions at all). Store-confirm gate kept (per Dilip: store stays a real participant). typecheck + agent/store builds pass; logged-in live fire is Dilip's browser step.
- 2026-06-27: **Agent alerts + admin agent-payouts/try-analytics/live-map** on branch `feat/admin-agent-payouts-analytics-alerts` (now that Jay's agent panel is on `main`). (1) **Agent pop-up alerts** reuse the Store/Admin realtime pattern but with 3 event types and **no client-side rider filter** — RLS (migration 014: `deliveries_select`, `try_sessions_select_rider`, `order_items_select_rider`) routes each rider only their own rows, same as Store relies on `order_items` manager-read. **Migration 020** adds `REPLICA IDENTITY FULL` on `deliveries` because the "new job" event is an *assign-UPDATE* (`rider_id` NULL→rider): realtime needs the full old+new image to evaluate the RLS visibility transition. (2) **Agent Payouts (#18)** is a near-clone of Store Payouts (#17) — new `agent_payouts` ledger (migration 020, mirrors `payouts`, keyed `(rider_id, order_id)`), payout basis = `orders.delivery_fee` per completed delivery (the only rider money the DB models, matching the agent Earnings screen). Razorpay disbursement stubbed. (3) **Try & Return Analytics (#10)** unblocked now that the agent loop writes `try_sessions`/decisions/`returns`; read-only, no migration. (4) **Live Map (#11)** uses a keyless `maps.google.com/maps?q=…&output=embed` iframe geocoded by the drop address text (no API key, no lat/lng) + a realtime active-deliveries board. typecheck + admin/agent builds pass; browser/live-fire verification is Dilip's step (needs migration 020).
- 2026-06-26: **Admin batch 2** (#20 Reports, #17 Store Payouts, #13 Complaints, #15 CMS) on branch `feat/admin-batch2` (stacked on `feat/admin-core4` + `feat/admin-notifications`). Migrations **012** (complaints) + **013** (content_blocks) — both idempotent, use `DO $$…duplicate_object` guards for enums and reuse `trigger_set_updated_at`. Store payout = Σ kept-item price × (1−commission), settled per-order into the existing `payouts` table; **real Razorpay disbursement is stubbed** (shared infra). Reports export client-side CSV via Blob. All audit-logged via `logActivity`.
- 2026-06-26: **Notifications & Alerts (#12)** — admin compose/send over the existing `notifications` table (no migration). `sendNotification` server action resolves recipients (all / by role / single user by email) and bulk-inserts one row per recipient via the service-role client, audit-logged via `logActivity`. Built on top of the Core 4 branch (PR #22) so it can reuse `logActivity`. First of the agent-independent "batch 2"; #17/#20/#13/#15 to follow.
- 2026-06-22: **Admin Payment Records (#9)** — read-only by design (no refund/edit action here; refunds would be a separate Razorpay flow). Summary totals computed server-side from the fetched rows. Added `success` + `initiated` styles to the shared `StatusBadge` (the `payment_txn_status` enum used them but they fell back to gray). Reused `DataTable`/`StatsCard`; row click routes to the existing `/admin/orders/[id]`.
- 2026-06-22: **Admin Activity Log (#21)** — introduced `apps/admin/lib/activity.ts` `logActivity(supabase, entry, actorId?)`: best-effort (wrapped in try/catch so a logging failure never breaks the underlying mutation), works with both the browser and SSR clients (admin session satisfies `activity_logs_admin` RLS), and accepts an explicit `actorId` for the service-role path (inventory server actions, which have no session). Wired into every admin mutation. `ip_address` is only captured server-side (null for client-side actions) — minor follow-up if full IP audit is needed.
- 2026-06-22: **User Role Management (#16)** — built now (not blocked on Jay after all): the `user_role` enum + `store_managers`/`riders` tables already exist, so role management needs no new schema. Role changes go through a guarded `changeUserRole` server action (service-role write + acting-admin id from the SSR session): blocks changing your own role, requires a store when assigning `store_manager` (upserts `store_managers`), auto-creates a `riders` profile, deactivates store assignments on demotion, and audit-logs via `logActivity`. The only deferred part is agent-panel-specific provisioning (assignments/deliveries), which is Jay's agent schema.
- 2026-06-03: Restructured the single Next.js app into a **pnpm monorepo** — one app per panel (`apps/{customer,agent,store,admin}`) + shared `packages/{supabase,ui,config}`. Admin is now a separate build/deploy so admin code & the service-role key never ship in the customer bundle. Kept the `/admin` route prefix inside the admin app to avoid rewriting ~45 links. History preserved via `git mv`. (branch `chore/monorepo-restructure`, PR #1)
- 2026-06-04: Fixed Tailwind v4 CSS-layering bug in customer `globals.css` — unlayered base resets (`a { color: inherit }`) were beating `text-white` on navy `<Link>` buttons; wrapped base resets in `@layer base`.
- 2026-06-04: Extracted size-chart data to `apps/customer/lib/sizeData.ts`, shared by the product SizeChartModal and the new `/size-guide` page.
- 2026-06-04: **Work split** — D wraps the Customer panel (standalone screens) and moves to the **Store panel**. Partner owns Razorpay/customer-loop finish + the Agent panel.
- 2026-06-16: **PRODUCT MODEL PIVOT** — dropped the "try-at-home for 24h + free pickup later" model. New model: customer **books a delivery slot**, the rider brings picks to the door and **waits 15–30 min** while they try on, customer keeps (pays) what they love and **hands the rest back to the rider on the spot** (no scheduled return). Swept all customer + store copy + docs (CLAUDE.md, README, `.claude/` commands). Try-window duration is now a `TRY_WINDOW_MINUTES = 30` constant in `checkout/actions.ts` + admin `OrderActions.tsx` (placeholder — should move to Admin settings and **start on rider arrival**, agent panel). "60-min delivery" replaced by slot-booking copy. (branch `feat/doorstep-try-model`)
- 2026-06-19: **Razorpay Keep payment (#13)** — customer pays **per item** (one Razorpay Checkout per kept item) at the moment they tap "Keep", chosen over a single pay-at-end. Settlement can't be a direct client write (customer app has only the anon key, no service-role), so a SECURITY DEFINER RPC `confirm_keep_payment` re-verifies the Razorpay HMAC in-DB (pgcrypto `hmac`, `search_path` incl. `extensions`) and is the only writer of `payment='success'`/`order paid`. Razorpay **secret lives in Supabase Vault** (`vault.create_secret('…','razorpay_key_secret')`) — Supabase blocks `ALTER DATABASE/ROLE SET` for the SQL-editor role, so GUCs don't work. Migrations **009** (RPC + `payments.order_item_id`) and **010** (see below).
- 2026-06-19: **Found migration 002's RLS policies were missing in the live DB** — `try_sessions`/`returns`/`payments`/`payouts` had RLS *enabled* but **zero policies** (deny-all), which broke checkout (`new row violates RLS for try_sessions`). Migration **010** idempotently recreates exactly those policies. (Migration 005 — re-enable RLS — had already been applied; this was a separate gap.)
- 2026-06-19: **Navbar auth was reading a mock localStorage flag**, not the Supabase session — replaced with `supabase.auth.getUser()` + `onAuthStateChange`. Checkout variant resolver made resilient (falls back to a product's first colour/variant) so an item added without a colour can't abort the order. Added a login-required modal + a checkout confetti celebration.
- 2026-06-19: **Try-window duration — to reconcile:** the doorstep pivot (PR #20) shipped `TRY_WINDOW_MINUTES = 30`; Jay had proposed ~5–7 min on-the-spot. Jay & Dilip to agree on the final value (then move it to Admin settings, see Known issues).
- 2026-06-21: **Try-window resolved to 7 min** — `TRY_WINDOW_MINUTES = 7` in checkout + admin, and `start_try_window` (migration 014) sets a 7-min deadline. The window now correctly **starts when the customer taps "Start try-on"** after the rider marks delivered (not at checkout). Still should move the duration to Admin `system_settings` eventually.
- 2026-06-21: **Agent panel (P2 core loop) merged directly to `main`** (owner-authorized, no PR) — `feat/agent-panel` fast-forwarded into main. Includes the `instrumentation.ts` SSR localStorage shim for both `apps/agent` and `apps/store`.
- 2026-06-21: **Killed the manual-SQL-per-test problem** — root cause was that agent signup only wrote `auth.users` (no `public.users`/`riders` row, so a new rider was invisible in Admin and couldn't be verified without a hand INSERT), plus migration 014 not always being applied (so the timer/RPCs were missing). Fix: migration **015** adds a `handle_new_user` trigger on `auth.users` that auto-creates the `public.users` row (role from signup metadata) + a `riders` row for riders, with a one-time back-fill. Admin already had the Verify-Rider + Assign-Delivery UI. End-to-end testing is now a click-path (`docs/HANDOFF-agent-testing.md`) with the only SQL being the one-time migration paste.

- 2026-06-22: **Admin System Settings (#19) made real** — added a `system_settings` **singleton** table (migration 011, `id smallint PRIMARY KEY CHECK (id=1)`) + RLS (`authenticated` SELECT so store Earnings / customer try-timer can read config; `is_admin()` for writes) reusing the shared `trigger_set_updated_at`. Try-window stored in **minutes** (1440 = legacy 24h) — chosen over hours so it survives the pending doorstep pivot to ~5–7 min. Writes go through the service-role admin client (stamping `updated_by`); reads via the SSR client. Screen split into a server `page.tsx` + `SettingsClient.tsx` per the admin convention. Consumers (Earnings commission math, customer countdown) still read hardcoded values — wiring them is follow-up.

## Realtime / cross-panel
> **⚠️ Architecture note (2026-06-27): cross-panel pop-up alerts now ride on the
> `notifications` table, NOT on direct table subscriptions with RLS routing.**
> Hard-won lesson: Supabase Realtime `postgres_changes` can only route an event to
> a subscriber whose RLS lets them see the row — and it CANNOT reliably evaluate
> (a) a **join-based** policy (store's `order_items_manager_select`, agent's
> `try_sessions`/`order_items` rider policies) or (b) an **UPDATE that moves a row
> *into* a user's visibility** (admin assigning `deliveries.rider_id`). Both fail
> silently → "alert only goes to admin / store stopped popping". The fix everywhere:
> a DB trigger inserts a **born-visible** `notifications` row owned by the exact
> recipient, who subscribes to `notifications` INSERT filtered to their own
> `user_id` (the plain `user_id = auth.uid()` policy Realtime routes for free).
> Admin still uses `orders` INSERT directly (its policy is the simple `is_admin()`,
> which Realtime handles). Customer order-tracking uses an `id`-filtered subscription
> on its own order (born-visible). `notifications` is on the publication via 021.
- [~] **Admin new-order alerts** — `orders` INSERT (simple `is_admin()` RLS, routes fine). Needs **migration 018**. Bell in the header row.
- [~] **Store new-order alerts** — D — `OrderAlertsProvider` reads the manager's own `notifications` (kind=`new_store_order`, from the **migration 022** trigger). ⚠️ Realtime alone did **not** deliver to store sessions (unlike admin/agent), so the primary mechanism is now a **poll of the notifications table every 8s** (seeded on first load so no backlog pop), with Realtime kept as an instant nudge; both dedupe by notification id → `loadStoreOrder` fills the summary. Reliable regardless of Realtime. **Built.**
- [~] **Rider self-serve offers (Agent)** — D — the core rework: no admin assignment needed. After the store confirms an order, its delivery is offered to **all online verified riders**. `IncomingJobsProvider` (mounted in `AgentShell`, active only while **Online**) polls `available_deliveries()` every 7s, shows an **Accept/Decline** offer card, and rings a **repeating chime until the rider acts** (driver-app UX). Accept → `rider_claim_delivery()` atomically claims it (first rider wins; losers get "just taken"). **Decline** records a 10-min cooldown (`rider_decline_delivery`, migration 025) so the job stops re-offering to that rider across polls/refresh while staying open to everyone else. **Migrations 024 + 025** (SECURITY DEFINER, verified-rider gated). Admin assignment still works as an override, and the admin **Deliveries page shows "N riders online" + "N waiting for a rider"**. **Built — live fire needs 024+025 + a verified rider going Online.**
- [~] **Live rider alerts (Agent)** — D — `JobAlertsProvider` (mounted in `AgentShell`, verified-riders only): pop-up + bell + chime for **new_job / try_started / item_kept / item_returned**, all read from the rider's own `notifications` stream (one subscription, `data.kind` discriminates). Pop-ups deep-link to `/deliveries/[id]`. Triggers: **migration 021** (assign→new_job) + **migration 023** (try-active→try_started, decision→item_kept/returned). **Built — live fire needs 021 + 023 + a real assigned delivery with the agent app open.**
- [~] **Store→Admin fulfillment connected** — admin order detail now shows the store's per-item "Ready" (`prepared_at`) + a "Store prep: X/Y ready" summary, and the **"Mark Out for Delivery" action is blocked until the store marks all items ready** (proper handoff, no jumping ahead). Also fixed the admin try-window display from hours → minutes (15–30 min model).

## Known issues / TODO
- **Migration 053 — apply after 052 (idempotent), G5 abuse caps (W4.6):** ⚠️ renumbering: M4 tax → **054**. Three `system_settings` knobs (0 disables): `max_items_per_order` (default 8, total UNITS in one order), `max_active_orders` (default **1** per the plan — one live doorstep try-on at a time; not-completed/not-cancelled counts, so return_requested/picked still block), `max_orders_per_day` (default 0/OFF — rolling 24h, cancelled EXCLUDED so store-no-confirm expiries don't eat a customer's quota; revisit if G4 self-cancel makes cancelling free). Enforced at the TOP of `place_order` (053 = 052's function + the caps block — apply order matters, 053 without 052 would drop the pause gate) with limit-carrying errors `ORDER_TOO_MANY_ITEMS/ORDER_LIMIT_ACTIVE/ORDER_LIMIT_DAILY:<n>` → friendly checkout copy. Admin → Settings → "Order Limits" section (pre-053 save shows a clean apply-053 error; other sections unaffected). ⚠️ Applying flips `max_active_orders=1` ON immediately — **test accounts with stale active orders will hit ORDER_LIMIT_ACTIVE on their next order; that's the cap working**, complete/cancel the stragglers or raise the setting. **Browser check after applying: (1) place an order, leave it active → second order blocks with "finish that doorstep try-on"; complete/cancel it → order flows; (2) 9 units in the bag → blocks with "at most 8 items"; (3) set daily cap 1 → second order in a day blocks; set 0 → unlimited again; (4) Settings saves all three; garbage (negative/decimal) rejected.**
- **Migration 052 — apply after 051 (coupons lockdown, PR #47) (idempotent), store Open/Paused (G6/W4.7):** ⚠️ renumbering: M4 tax → **053**. (1) `stores.is_paused` (manager-controlled; deliberately separate from `is_active`, the ADMIN kill switch a manager must never flip back on). (2) `store_set_paused(p_store_id, p_paused)` — guarded SECURITY DEFINER RPC (008 pattern: no UPDATE policy on `stores`), approved stores only. (3) `place_order` recreated (050 verbatim) + gate: items from a paused store raise `STORE_PAUSED:<store name>` → checkout shows "temporarily closed, try later". Paused stores + catalogues stay VISIBLE (Swiggy-closed pattern); only NEW placement is blocked — orders already placed still flow. Store dashboard shows the Open/Paused card only when the column exists (pre-052 → hidden, no dead switch). The G6 serviceability half is app-layer (no migration): admin approval checks the store pincode against `@fitzo/pincode` and demands an audit-logged "Approve anyway" override outside the list. **Browser check after applying: (1) store dashboard shows "Store open" card → Pause → customer checkout on that store's product errors with "temporarily closed" and no order row is created; (2) Resume → order goes through; (3) paused store's catalogue still browsable; (4) admin: give a test store pincode 110001 → Approve asks for override + activity log entry carries serviceability_override; 411001 → approves clean.**
- **Migration 050 — apply after 049 (idempotent), delivery fee UPFRONT (G9/W3.6; closes G8):** ⚠️ renumbering: M4 tax → **051**. The fee becomes its own Razorpay payment right after checkout — collected on keep AND return-everything (the return-all free-trip loss is dead). (1) `system_settings.first_order_free` (default OFF) — first non-cancelled order ships free; admin Settings gains the toggle + relabels Free-Delivery-Above as **KEPT-value**. (2) `place_order` recreated: ordered-subtotal waiver REMOVED (G8 closed), first-order-free added; try-session duration now also read from settings (048 parity). (3) `settle_keep_payment` recreated with ONE guard: the order-paid rollup runs only for item payments (a fee-only settle must not flip a fresh order to 'paid'). (4) `store_confirm_order` recreated + gate: `DELIVERY_FEE_UNPAID` while fee due (store panel maps it to friendly copy) — fee-dodgers who close the modal never get a trip; 036's expiry sweep cancels them on its normal clock and 047's trigger frees the stock. (5) NEW `record_delivery_fee_refund` RPC — bookkeeping half of the kept-value waiver: money moves FIRST via Razorpay's refund API in the customer app's server action (`refundDeliveryFeeIfEligible`, fired after the last keep/return decision + a tracking-page catch-up for rider-completed orders), then the RPC flips the ledger row after re-verifying in-DB (owner, all-decided, kept-paid ≥ threshold, standalone fee payment only). Composition: the upfront payment carries `delivery_fee_component`, so 040's carried-check auto-skips the first-Keep fold, and order_economics is already refund-aware. Customer UI: tracking-page "Pay ₹49 delivery fee" card (reuses the keep-payment modal + settle path) + green refunded note + checkout copy ("refunded when you keep ₹999+", "first order free"). Everything probes 050 and degrades to exact 040/049 behavior pre-apply. **Browser check after applying: (1) place order → tracking shows the pay-fee card; store "Ready & confirm" BLOCKS with friendly copy until paid; (2) pay fee → store can confirm; first Keep charges bare item price; (3) keep ≥ ₹999 → fee auto-refunds (green note + Payments row flips refunded + Money card fee-collected drops); (4) return everything → fee stays collected (margin shows it); (5) toggle first-order-free + fresh account → checkout shows ₹0 fee.**
- **Migration 049 — apply after 048 (idempotent), place_order carries the address (A1 × G3):** reconciles 047's `place_order()` with the A1 address work that reached main separately. Drops the 2-arg `place_order` and recreates it as `place_order(p_items, p_payment_method, p_address_id)`: requires `p_address_id`, verifies the address belongs to `auth.uid()` (RAISE `ADDRESS_INVALID` otherwise — mandatory since the RPC is SECURITY DEFINER), and stamps `orders.address_id` so the 014 delivery trigger snapshots the drop address. Everything else (G2 pricing, G1 single-store, G3 reservation, release triggers) unchanged. `checkout/actions.ts` passes `p_address_id` and keeps the address+G2 legacy path as the PGRST202 fallback. Pincode serviceability stays in `lib/pincode.ts` (app layer). **Browser check after applying: place an order → the new order row carries a non-null `address_id`, and `deliveries.drop_address` is populated for the rider; a request with someone else's/absent address → friendly "delivery address could not be found".**
- **Migration 047 — apply after 046 (idempotent), stock reservation (G3/W2.8):** ⚠️ renumbering: M4 tax provisions move to **048**. `place_order(p_items, p_payment_method)` RPC becomes the one way orders are created: resolves variants (an explicitly chosen size that's out of stock ERRORS — no silent substitution), **prices from the DB (G2 — client price never trusted)**, **single-store cart enforced (G1: `MULTI_STORE_CART`)**, product-active/store-approved checks, then reserves stock under `FOR UPDATE` row locks (`reserved_qty += units` only when `available_qty` suffices) and creates order + per-unit items (`stock_reserved=true`) + try session atomically. Releases are **transition triggers**, so every existing path restores stock with zero edits: item decision pending→keep = `stock_qty−1, reserved−1` (sold), pending→return = `reserved−1` (back on shelf) — catches settle_keep_payment/returnItem/auto-return 027; order →cancelled releases all still-reserved units — catches admin Cancel/rider_fail 033/expire_stale_offers 036. Idempotency = `order_items.stock_reserved` flag; pre-047 orders (flag false) are untouched by every branch. `placeOrder` tries the RPC and **falls back to the legacy client-insert path on PGRST202** (pre-047 = old behavior exactly). **Browser check after applying: place an order → variant's `available_qty` drops; keep one item → `stock_qty` drops too; return the other → `available_qty` comes back; cancel a fresh order → reservation released. Then try a 2-store cart → friendly single-store error.**
- **Migration 046 — apply after 045 (idempotent), commission stamping + per-store rate (money M3):** commission stops being computed from the LIVE settings rate (which let a Settings change silently rewrite historical payables). Adds `stores.commission_rate` (NULL = platform default; admin Store-detail "Commission override (%)" field, degrade: pre-046 the rest of the form saves and the rate shows a clean apply-046 error) + `order_items.commission_rate`/`commission_amount` stamped inside `settle_keep_payment` at the moment a Keep settles (client + webhook paths both stamp; resolution: store override → settings → 15; fill-only). **Backfill freezes all existing kept-and-paid items at today's effective rates** — same rupees everywhere, now immutable. `order_economics` + `store_order_economics` recreated to sum stamped amounts (store-aware live-rate fallback only for unstamped edge cases); the Money card derives the displayed % from amounts (commission/kept_paid), so mixed-rate orders show the blended truth. **Browser check after applying 046: (1) all money screens show identical rupees to before; (2) set a store override (e.g. 12%) → old orders unchanged, a NEW settled keep stamps 12%; (3) change the Settings rate → historical payables don't move.**
- **Migration 048 — CLAIMED by J (W1.5/A3), apply after Dilip's reserved block 043–047 (idempotent), try-window single source of truth:** re-creates `start_try_window()` to read `system_settings.try_window_minutes` (fallback 7, same owner-gate/contract as 014) and corrects the live setting **60 → 7** (guarded — 60 was a mix-up with the 60-minute delivery promise; owner decision Jay 2026-07-15). App-side hardcoded 7s in checkout + tracking/confirmation copy repointed to the setting on branch `feat/customer-trywindow-A3`. Pre-048 the app already degrades fine (checkout writes a setting-based placeholder deadline; the RPC just keeps using 7).
- **Migration 045 — apply after 044 (idempotent), `store_order_economics()` (money M2, part 2 — completes W2.7):** guarded SECURITY DEFINER RPC returning the per-(store, order) slice of the money truth — needed because store payables/earnings have a store dimension the 044 view lacks, and a security_invoker view can't work for store managers (they can't SELECT customer `payments`, so kept-paid would read 0 for them). Gate: `p_store_id=NULL` → admin only (all stores); given → `is_store_manager_of` or admin. Semantics identical to 044 (kept-and-paid basis, refund-aware). **Consumers repointed:** Admin > Store Payouts `computeStorePayables` (record-payout action therefore stops owing refunded orders), store Earnings `loadPayoutSummary` (+ "Refunded / unpaid (excluded)" breakdown line when non-zero), admin dashboard Today's-Revenue + weekly chart and Analytics 30-day revenue → `order_economics.net_captured` ("Captured − refunds" subtitles). All degrade pre-045 to the old math (RPC error → legacy path). Agent-payouts compute deliberately NOT repointed: rider pay = flat `rider_fee` per completed delivery, refund-independent — already matches the view's `rider_cost` by construction. ⚠️ **Renumbering: M3 commission-stamping → 046, M4 tax provisions → 047.** **Browser-verified live (dashboard ₹958 today / analytics ₹7,553 = Total Captured); store Earnings + payouts RPC path needs 045 applied — after applying, Store Payouts and store Earnings should show the same rupees as before (no refunds exist yet), just refund-proof.**
- **Migration 044 — apply after 043 (idempotent), `order_economics` view (money M2, part 1):** ONE per-order money truth — refund-aware (revenue keys on live SUCCESS payments, not item decisions: a 041 refund drops the item from kept_paid_gross/commission/store_net automatically), gateway-aware (`gateway_cost` sums fees over success+refunded — Razorpay keeps fees on refunds), delivery-fee-collected from `delivery_fee_component`, rider cost gated on delivery COMPLETED, `margin = commission + fee_collected − rider_cost − gateway_cost`. `security_invoker` — caller's RLS applies, no widening. Admin order-detail Money card now reads it (falls back to the old inline math pre-044) with new lines: Refunded, kept-unpaid-excluded, fee collected-vs-charged, Gateway fees (with "partial" flag → run Sync on Payments). **Acceptance check after applying: FTZ-2026-00052 should show captured 958 / fee collected 49 / gateway −19.16 / commission 136.35 / store net 772.65 / rider ₹0 (not completed yet) / margin 166.19.** Remaining M2 (W2.7): repoint payouts compute, store Earnings, dashboard, analytics.
- **Migration 043 — apply after 042 (idempotent), gateway-fee capture (money M1):** adds `payments.gateway_fee` + `gateway_tax` (rupees; Razorpay's `fee` INCLUDES the GST, `tax` is the GST portion, fees are NOT returned on refund) and re-creates `razorpay_webhook_captured` to stamp them from the payload — including on the `already_settled` path, since the client settle never sees fee data (fill-only, never overwrites). Admin > Payments gains a **"Sync gateway fees"** backfill button (Razorpay Payments fetch API, batches of 50, audit-logged) + a small "fee −₹X" line under Amount. Everything degrades pre-043: the page queries the column separately (renders without it), the sync button pre-flights the columns before calling Razorpay. **Browser check after applying 043: hit "Sync gateway fees" → the 10 existing Razorpay rows gain fee lines; margins in M2's order_economics will read these.**
- **Migration 042 — apply after 041 (idempotent), traceable manual payouts:** adds `reference` (UTR/UPI txn id) + `paid_to` (masked destination snapshot) to `payouts` AND `agent_payouts`; both record-payout modals gain the reference field + honest "make the transfer yourself" copy. Degrades pre-042 (insert retries without the new columns). Real disbursement: see `docs/PAYOUTS-GOING-LIVE.md`.
- **Migration 041 — apply after 040 (idempotent), admin refunds:** adds `payments.razorpay_refund_id/refunded_at/refund_reason`. Admin > Payments gains a per-row **Refund** action (full refund via Razorpay's refund API; success rows only). Needs `RAZORPAY_KEY_ID` + `RAZORPAY_KEY_SECRET` in `apps/admin/.env.local` (already copied from the customer app's; remember BOTH apps when Jay rotates the leaked test secret). The action pre-flights the 041 columns BEFORE calling Razorpay so a pre-migration click can't move money it can't record.
- **Migration 040 — apply after 039 (idempotent), delivery fee on first Keep:** adds `payments.delivery_fee_component`. The customer's `orders.delivery_fee` is now actually collected — folded into the FIRST Keep charge on the order (owner decision 2026-07-10); returns-everything = fee uncollected (accepted). Everything degrades to the old bare-item charge pre-040 (the fee check errors → fee 0, verified against the live DB). Tracking page shows a "fee added to your first Keep payment" note + the Razorpay modal description carries the split.
- **Migration 039 — apply after 038 (idempotent), payment.captured webhook:** extracts 009's settle into `settle_keep_payment()` (internal, no grants), recreates `confirm_keep_payment` as a wrapper (contract unchanged), adds `razorpay_webhook_captured()` (in-DB webhook-HMAC verification via new Vault secret `razorpay_webhook_secret`). **Needs the one-time manual steps in 039's header (Dilip — owns Razorpay):** Razorpay dashboard webhook (URL + secret, event `payment.captured`) + `vault.create_secret` + `RAZORPAY_WEBHOOK_SECRET` in `apps/customer/.env.local` (already generated there). Route degrades safely pre-039 (500 → Razorpay retries).
- **Migrations 037 + 038 — apply in order after 036 (idempotent), rider-pay decoupling:** `orders.delivery_fee` did double duty (customer charge AND rider pay), so free-delivery orders (≥ `free_delivery_above`) left riders earning ₹0. **037** adds `system_settings.rider_fee` (default 40) + `orders.rider_fee` (stamped at checkout, always paid, no free-delivery waiver) + backfills existing orders; agent Earnings/History + Admin > Agent Payouts read `orders.rider_fee`; `orders.delivery_fee` stays the customer charge; Admin → System Settings → Commission & Payouts exposes "Rider Pay per Delivery". **038** repoints `available_deliveries()` to return `rider_fee` (drops+recreates the function — return column renamed `delivery_fee`→`rider_fee`) so the **offer card shows the rider's actual pay**, not the customer's ₹0-on-free charge; agent client reads `d.rider_fee ?? d.delivery_fee` (works pre-038 too). ~~One follow-up left: the admin order-detail money card still counts `delivery_fee` as the rider cost in its margin~~ — **fixed 2026-07-10** (branch `feat/money-hardening`): the Money card's rider cost reads `orders.rider_fee`, and margin = commission + delivery charge − rider pay (caveat notes prepaid delivery-fee collection is still pending).
- **Migration 036 — APPLIED 2026-07-09 (idempotent), reliability:** stale confirmed-but-unclaimed orders stop ringing riders. Adds `system_settings.offer_expiry_minutes` (default 120), a freshness filter on `available_deliveries()` (only offers orders placed within that window), and `expire_stale_offers()` — a pg_cron sweep that cancels genuinely-abandoned orders + notifies the customer (born-visible notification). Optional pg_cron block schedules both this and 027's `expire_try_windows`. Agent offer card's "waiting X min" label is now capped (hours/days) so it never reads "waiting 25512 min". **Freshness measured from `orders.created_at`** (no `confirmed_at` column exists — a precise "offered-at" clock is a future refinement; fine at current scale). **Admin → System Settings → Delivery & Try-On now exposes `offer_expiry_minutes`** (tunable without SQL, validated ≥5 min).
- **AUDIT 2026-07-10 (repo vs GitHub reconciliation):** (1) **PROGRESS missed three merged PRs + one open PR** — #34 seed test data (5 stores / 50 products / 4 riders, login-able), #35 store-onboarding Check-status + clearable notification bells, #36 **offer expiry** (migration **036** + `offer_expiry_minutes` in System Settings — fixes the "waiting 25512 min" stale-offer issue from the 07-07 live fire), and **open PR #37** (decouple rider pay from customer delivery charge, migration **037** — addresses the ₹0-rider-pay issue). Local `main` is behind origin/main; migrations 036–037 aren't in the local checkout. (2) `apps/customer/app/checkout/actions.ts:136` still hardcodes `TRY_WINDOW_MINUTES = 7` — must read `system_settings.try_window_minutes` (the admin setting exists since migration 011 but this consumer was never wired). (3) **No Razorpay webhook anywhere in the repo** (grep-verified) — payment confirmation is client-success-handler only; a `payment.captured` webhook is a production blocker. (4) Checkout still collects **pincode only, no street-address form** — riders get placeholder drop addresses (07-07 improvement list, still true). (5) RLS can't be verified from the repo — given the 2026-06-08 incident (RLS silently disabled in the live DB), re-run the `pg_tables.rowsecurity` check before launch. (6) Placeholder routes still shipping in customer: `/try-timer`, `/ai-style-setup`, `/reviews`, `/order-tracking` (index) all render `RoutePlaceholder`.
- **UI refresh follow-ups (2026-07-09):** (1) homepage promo banners are dummy consts in `PromoBanners.tsx` — build the `banners` table + Admin screen and read from DB (owner-approved next step); (2) /products filter rail has **no Size filter** — `queryProducts` can't filter by `product_variants` size yet (deliberately not faked); (3) customer app has **no ESLint config** (`next lint` drops into interactive setup); ~~no test runner~~ **vitest added 2026-07-10** (3 tests on `lib/sort.ts` — keep adding per screen); (4) ~~category chips only show genders present in `categories`~~ chips are now a curated set (2026-07-10, owner-approved) — but women/kids/footwear land on the full catalogue until those `categories` rows exist in Admin; (5) Sale chip awaits a clean brand-free image from Jay (his first pick was another mall's ad — see Decisions 2026-07-10).
- **Migration 035 — APPLIED 2026-07-07 (idempotent):** rider completion after the customer's last decision. `finalize_order_if_decided` (019) completes the ORDER but not the DELIVERY; 033's completion guard then left the delivery stuck at 'arrived' (rider uncredited, 1-job cap blocked). 035 lets `rider_complete_delivery` also close out from order-status 'completed' (nothing to auto-return — the rider just confirms collection). Found + fixed live in the 2026-07-07 full-loop test (FTZ-2026-00049).
- **Migration 034 — apply after 033 (idempotent), agent rework Phase 3:** `rider_payout_details` (bank/UPI + PAN, private table — RLS own-rider-or-admin, mirrors `store_business_details`) + guarded `save_rider_payout_details` RPC (in-DB format validation: PAN/IFSC/UPI/account-number). Agent Settings has the form, Earnings shows the missing-details nudge, Admin > Agent Payouts shows the destination — all degrade gracefully pre-034 (destination shows "No payout details").
- **Migration 033 — apply after 032 (idempotent), agent rework Phase 2:** pickup store stamped onto deliveries (`store_confirm_order` now writes `pickup_address` + a 4-digit `delivery_otp`), offer feed v3 (`available_deliveries`: 1-job cap, PII-redacted `drop_area`, store name/area/count — **return shape changed**, old `drop_address`/`final_amount` gone), new `rider_mark_arrived`, **`rider_mark_delivered(uuid)` DROPPED → `(uuid, text)` with OTP** (agent client falls back pre-033), pickup gated on store `prepared_at`, `rider_complete_delivery` status-guarded (audit D2), new `rider_fail_delivery(reason)` → files into Admin > Complaints, `get_delivery_handover` (customer reads OTP). The agent + customer apps degrade gracefully until it's applied (no OTP step, no store on offers).
- **Migrations 026–028 — apply in order after 025 (idempotent):** **026** fixes `available_deliveries()` (used a non-existent `deliveries.created_at`; now the order's). **027** try-window expiry (`auto_return_pending_items`, `rider_complete_delivery` auto-returns, `expire_order_if_due` client self-heal, `expire_try_windows` sweep + optional pg_cron). **028** stuck-delivery recovery (`rider_release_delivery`, `rider_set_availability` auto-releases un-picked-up jobs on Offline).
- **Migration 025 — apply after 024 (idempotent):** `delivery_declines` table + `rider_decline_delivery()` RPC + rebuilds `available_deliveries()` to hide a rider's declined jobs for a 10-min cooldown. Required for the agent Decline button to stick across polls/refresh.
- **Migration 024 — apply after 023 (idempotent):** `available_deliveries()` + `rider_claim_delivery()` RPCs for rider self-serve claiming (no admin assignment needed). Required for the agent "incoming offers" flow.
- **Migrations 020–023 — apply in order, all idempotent:** **020** `agent_payouts` table (+ RLS) for Admin Agent Payouts. **021** assign→`new_job` notification trigger + puts `notifications` on the realtime publication (required for ALL the new pop-up alerts). **022** order-line→`new_store_order` notification trigger (fixes the **store** pop-up). **023** try-active + keep/return → rider notification triggers (agent try-window pop-ups). Apply 021 before 022/023 (they all rely on `notifications` being on the publication, added in 021).
- **Try-window EXPIRY — now handled (migration 027).** `rider_complete_delivery` auto-returns undecided items; `expire_order_if_due` self-heals from the customer tracking page when the countdown hits 0; `expire_try_windows()` sweep is available for pg_cron (commented schedule in 027) for the fully-abandoned case.
- **Stuck deliveries — now recoverable (migration 028).** Rider "return to pool" (accepted-only) + auto-release un-picked-up jobs on going Offline + admin Deliveries "Release" button.
- **Delivery fee — now wired (#1, 2026-06-28).** `placeOrder` reads `system_settings.delivery_fee` + `free_delivery_above` and stamps `orders.delivery_fee` (free above the threshold) into `final_amount`; the checkout PriceSummary shows the same line. **Riders now earn it** (agent payouts already read `delivery_fee`). Rider pay = **full delivery fee** (no separate rider-commission field yet). ~~Collection caveat: the fee was never actually charged to prepaid customers~~ — **fixed 2026-07-10 (migration 040):** the fee is folded into the first Keep charge (see Known issues / decisions log). Also, the checkout *display* only resolves the fee once logged in (system_settings is authenticated-read); the server calc at placement is always correct.
- **Rider offers: no geo / no concurrency cap** — every online rider sees every waiting job and can claim unlimited. Fine at test scale; add "hide jobs from a rider with an active delivery" + nearby-only before scaling.
- **Model pivot follow-ups (2026-06-16):** (1) the functional **delivery-slot picker** isn't built — copy now promises slot booking but Checkout still has no slot UI (maps to customer #8). (2) The **try window still gets its deadline at checkout** (30-min placeholder); it must start when the **rider confirms delivery** (agent panel) — until then the tracking countdown is a placeholder. (3) the try-window duration now lives in Admin settings (`system_settings.try_window_minutes`, migration 011) — but the customer countdown still uses a hardcoded constant and must be wired to read it.
- **Commission/system-settings storage now EXISTS (2026-06-22, migration 011)** — the `system_settings` singleton + the real admin System Settings UI persist `commission_rate` + `try_window_minutes` (RLS: authenticated read / admin write). Still TODO — wire the **consumers**: store Earnings should compute commission off `commission_rate` (today shows gross only), and payout computation (ties into Razorpay Payouts, partner/admin scope) is unbuilt.
- 🔴 **SECURITY (2026-06-08): RLS was DISABLED on `users`, `orders`, `order_items` in the live DB** — anon key + no session could read all customer PII (names/emails/phones) + all orders. Found while building the Store Dashboard (dashboard showed 6 orders with no session). Policies in schema.sql are intact; RLS had just been turned off. Fix = run **migration 005** (`packages/supabase/migrations/005_reenable_rls.sql`, re-enables RLS on all tables, idempotent). **Affects the customer panel too — apply ASAP.** Verify with `SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname='public';`
- **Store panel:** migration **004** (`004_store_manager_read.sql`) adds manager SELECT on products/variants/orders/order_items/returns — required for Dashboard + Orders/Returns/Earnings. Apply before testing the store dashboard.
- **Customer loop:** Payment #13 **done** for the keep path (per-item Razorpay). Delivery-slot booking (#8) is now core and still pending; scheduled Return Pickup (#14) is largely obsolete under the new model. Still pending: Razorpay **payouts**. The `payment.captured` **webhook is built** (2026-07-10, migration 039 pending apply + dashboard setup — see Known issues), closing the "tab closed right after paying → order never marked paid" hole once applied.
- **Razorpay env:** test keys live in `apps/customer/.env.local` (gitignored — NOT in git). Any teammate must add `NEXT_PUBLIC_RAZORPAY_KEY_ID` + `RAZORPAY_KEY_SECRET` to their own `.env.local`. **Regenerate the test secret before going live** (it was shared in chat). At deploy, swap to live keys + update the Vault secret + add a verified-website + webhook.
- ~~Store-manager RLS for viewing orders that contain their products likely needs a policy~~ **RESOLVED** — migration 004 added manager SELECT via the `products.store_id` join; the whole store panel ships on it.
- Automated tests: **first 20 landed 2026-07-02** (vitest, `apps/store` pure logic — validators/slug/image checks). Everything else is still untested; keep adding smoke tests per screen via `/finish-task`.
- Admin has no 2FA (spec requires it for admin login).
- AI skin-tone endpoint not built; Product Detail badge + AI Style Setup are placeholders.
- Hosting provider not chosen (does not affect code; decide before Week 8).
- ~~`agent` app is still an empty shell; `store` app is now in active development~~ **STALE (removed 2026-07-10 audit)** — agent is a full 13-route rider app (rework merged via PR #33); store panel is complete.
