import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function TermsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Terms"
      title="Terms that support try-before-you-buy shopping."
      description="Use this page as the legal destination for checkout, returns, and payment conditions as the product grows."
    />
  );
}
