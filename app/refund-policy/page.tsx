import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function RefundPolicyPage() {
  return (
    <RoutePlaceholder
      eyebrow="Refund policy"
      title="Clear returns and refund expectations for doorstep try-ons."
      description="This route is now wired from the footer so refund questions no longer dead-end."
    />
  );
}
